# 20260912 归档

> 本文件由 `toolkit tasks archive` 自动生成。

## 20260912-唐启云-统一技能状态措辞与JSON输出

> 负责人：唐启云　状态：已完成　范围：toolkit　完成时间：2026-09-12 16:51

# 统一技能状态措辞与 JSON 输出

延续 收窄技能状态分类口径（见 archive/202609/20260912.md）

## 背景

复检留有两个非阻塞观察项：

1. **状态名两种叫法**：`skills status` 的 help 描述、`docs/handbook.md`、`skills/README.md` 三处写「冲突」，而汇总行与 `docs/cli.md` 写「同名冲突」；且这三处列表只罗列状态名，未体现「副本漂移裸 `install` 可修、同名冲突需 `--force`」的分野，用户看 help 无法判断该跑哪条命令。
2. **输出选项不对称**：`skills path` 有 `--json`、`tasks` 有 `--format json`，唯独 `skills status` 只有人读文本——巡检 / CI 想稳定消费现场只能正则解析；同时 `--json` 与 `--format json` 两种写法并存。

`skills` 命令域随 1.9.0 新增且尚未发布，选项更名无兼容负担。

## 目标

- 状态名全仓统一为「同名冲突」；三处列表补上处置分野（前四类裸 `install` 可修、同名冲突需 `--force`）。
- `skills status` 补 `--format json`（stdout 只出机器可读报告）；`skills path --json` 收敛为 `skills path --format json`，与 `tasks` 同口径。

## 实施方案

1. `src/cli.ts`：`skills status` 增 `--format <format>`（仅支持 `json`，非法值告警退出码 1；`json` 时输出 `skillsStatus()` 报告对象，默认仍为人读视图）；`skills path` 的 `--json` 改为 `--format <format>`；两处 help 描述里的「冲突」改为「同名冲突」。
2. `src/__tests__/skills.test.ts`：补现场报告 JSON 契约用例（字段齐备、`conflict` 态可序列化读出）。
3. `docs/cli.md`：示例行 `path --json` → `path --format json`；`status` / `path` 行为表补 `--format json` 说明。
4. `docs/guide.md`：`skills path --json` → `skills path --format json`。
5. `docs/handbook.md`、`skills/README.md`：状态名统一为「同名冲突」，并补处置分野。
6. `.changeset/minor-skills-bundled-distribution.md`：`path [--json]` → `[--format json]`，并补 `status [--format json]`。

## 影响范围

| 类型 | 路径 | 触达情况 |
| --- | --- | --- |
| 代码 | `src/cli.ts` | 已改：status 增 `--format`、path 换 `--format`、help 措辞统一 |
| 代码 | `src/__tests__/skills.test.ts` | 已改：新增 JSON 契约用例 |
| 文档 | `docs/cli.md` | 已同步：示例与行为表补 `--format json` |
| 文档 | `docs/handbook.md` | 已同步：状态名统一 + 处置分野 |
| 文档 | `docs/guide.md` | 已同步：`path --json` → `path --format json` |
| 文档 | `README.md` | 无触达：仅列 `toolkit skills install/status/remove` 命令名，无状态名与输出选项 |
| 文档 | `docs/getting-started.md` | 无触达：仅列命令名，无状态名与输出选项 |
| 文档 | `docs/faq.md` | 无触达：未涉及状态名与 `skills` 输出选项 |
| 文档 | `docs/api.md` | 无触达：未涉及 `skills` 命令 |
| 文档 | `docs/ai-rules.md` | 无触达：薄壳稳定纪律，不含状态名与输出选项 |
| 文档 | `docs/config.md` | 无触达：skills 段只讲 autoLink 两开关，未涉状态名与输出选项 |
| skills | `skills/README.md` | 已同步：状态名统一 + 处置分野（无 frontmatter，无 version 字段） |
| skills | `skills/fxri-*/SKILL.md` | 无触达：未提及状态名与 `skills` 输出选项，故不递增 version |
| 变更集 | `.changeset/minor-skills-bundled-distribution.md` | 已补：`--format json` 口径 |

## 验证与回滚

- 验证：`pnpm typecheck` → `pnpm build` → `pnpm test`（含新增用例）→ `toolkit tasks check`；实机跑 `toolkit skills status --format json`（可解析、含 `conflict` 态）与 `toolkit skills status --format xml`（告警 + 退出码 1），`toolkit skills path --format json` 正常、`--json` 已不被识别。
- 回滚：`--format` 选项与 help 措辞为独立改动，还原 `src/cli.ts` 两处 action 与文档三行即回到旧口径；`skills` 域未发布，无兼容残渣需处理。

## 执行结果

- 质量门：`pnpm typecheck` 通过；`pnpm build` 通过（ESM `dist/cli.js` 78.60 KB、CJS 79.87 KB）；`pnpm test` 21 文件 / 178 用例全过（新增 1 例 JSON 契约用例）；`toolkit tasks check` error 0 / warn 0；`toolkit tasks normalize` 0 问题。
- 实机核对（`node dist/cli.js`）：`skills status --format json` 退出码 0，输出可 `JSON.parse`，字段为 `source` / `skills` / `stateFile` / `stateExists` / `targets` / `pendingAgents`，本机现场读得 `conflict` 与 `link` 两态；`skills status --format xml` 打印「⚠️ 不支持的输出格式「xml」，仅支持 json」并退出码 1；`skills path --format json` 退出码 0，键为 `package` / `source` / `skills`；`skills path --json` 报 `unknown option '--json'` 且退出码 1。
- 三方一致：`docs/cli.md`（示例两行 + `status` / `path` 行为表）、`docs/handbook.md`、`skills/README.md`、`.changeset/minor-skills-bundled-distribution.md` 均改为「同名冲突」并补「前四类裸 `install`、同名冲突需 `--force`」分野，与 `src/cli.ts` 的 help 描述、汇总行及实现逐字对应；`README.md`、`docs/getting-started.md`、`docs/faq.md`、`docs/api.md`、`docs/ai-rules.md`、`docs/config.md`、`skills/fxri-*/SKILL.md` 复核无触达（仅出现命令名，不含状态名与输出选项），故未递增技能 `version`。
- 校正一处：JSON 报告的顶层字段是技能**源目录**（`source`）而非包根（包根只在 `skills path --format json` 的 `package` 键里），`docs/cli.md` 示例注释据此改为「技能源目录、状态文件、技能清单、各目标逐技能状态」。
- conventions 同步：本改动未触及 `AGENTS.md`、`docs/ai-rules.md` 与 `skills/fxri-*/SKILL.md` 条文，`.tasks/conventions.md` 溯源索引无需变更；第 12 条（stdout 只承载机器可读内容，如 `--format json`）与本改动同向，收窄而非改义，索引内「当前语义」维持原文。

---

## 20260912-唐启云-收窄技能状态分类口径

> 负责人：唐启云　状态：已完成　范围：toolkit　完成时间：2026-09-12 16:44

# 收窄技能状态分类口径

延续 优化skills自愈并新增开关（见 archive/202609/20260912.md）

## 背景

复检发现 `skillsStatus()` 对「同名实体目录」不做来源区分：只要内容与真源不一致就判 `copy-drift`（副本已漂移）。但两类现场处置手段不同——状态文件登记为本包副本的漂移，裸 `toolkit skills install` 即可刷新；上游安装器留下的、未登记为本包产物的同名目录，裸 `install` 会按冲突跳过、必须 `--force`。对外口径因此错位两处：

1. `docs/cli.md` `install` 行只写「同名实体目录默认跳过」，漏了同名普通文件同样默认跳过（实现见 `skills.ts` 的 `conflicts` 分支）。
2. `status` 汇总把 `copy-drift` 与 `conflict` 一并导向 `install --force`——对已登记副本的漂移属过度收敛（保守但口径不精确）。

## 目标

- 分类口径按「状态文件是否登记为本包副本」二分：**已登记且内容不一致 → `copy-drift`**（裸 `install` 可刷新）；**未登记同名目录且内容不一致 → `conflict`**（需 `--force`）。
- 对外文案（status 汇总 / `docs/cli.md`）与实现、测试三者严格一致。

## 实施方案

1. `src/skills.ts` `skillsStatus()`：`kind === "dir"` 且内容与真源不一致时，按该目标的状态记录 `copies` 是否含此技能名分流——已登记 → `copy-drift`；未登记 → `conflict`。
2. `src/cli.ts` status 汇总改为新分型：`缺失 / 悬空 / 指向错误 / 副本漂移` → `install` 补齐；`同名冲突` → `install --force` 覆盖。
3. `src/__tests__/skills.test.ts`：新增「未登记同名目录报同名冲突、裸 install 仍跳过、`--force` 才覆盖」用例。
4. `docs/cli.md`：`install` 行补「/ 普通文件」；`status` 行按新分型改写末尾指引。
5. `.changeset/patch-skills-autolink-tuning.md`：补记 status 分类口径修正。

## 影响范围

| 类型 | 路径 | 触达情况 |
| --- | --- | --- |
| 代码 | `src/skills.ts` | 已改：`skillsStatus()` 目录态分流 |
| 代码 | `src/cli.ts` | 已改：status 汇总分型文案 |
| 代码 | `src/__tests__/skills.test.ts` | 已改：新增未登记同名目录用例 |
| 文档 | `docs/cli.md` | 已同步：`install` 行补「/ 普通文件」，`status` 行按新分型改写 |
| 文档 | `README.md` | 无触达：仅列 `toolkit skills install/status/remove` 命令名与查询作用，未描述状态分类口径 |
| 文档 | `docs/getting-started.md` | 无触达：未涉及 skills 状态分类 |
| 文档 | `docs/handbook.md` | 无触达：仅罗列 5 个问题态名称，本次未改状态名集合 |
| 文档 | `docs/guide.md` | 无触达：未涉及 skills 状态分类 |
| 文档 | `docs/faq.md` | 无触达：未涉及 skills 状态分类 |
| 文档 | `docs/api.md` | 无触达：未涉及 skills 状态分类 |
| 文档 | `docs/ai-rules.md` | 无触达：薄壳稳定纪律，不含状态分类细节 |
| 文档 | `docs/config.md` | 无触达：skills 段只讲 autoLink 两开关，未涉及状态分类 |
| 文档 | `skills/README.md` | 无触达：仅罗列状态名；`--force` 覆盖同名非本包产物表述准确 |
| skills | `skills/fxri-*/SKILL.md` | 无触达：仅提及 `skills install` / `skills status` 存在，未描述分类口径；无内容变更故不递增 version |
| 变更集 | `.changeset/patch-skills-autolink-tuning.md` | 已补记口径修正 |

## 验证与回滚

- 验证：`pnpm typecheck` → `pnpm build` → `pnpm test`（含新增用例）→ `toolkit tasks check`；构造未登记同名目录与已登记漂移副本两种现场，核对 status 分别报「同名冲突」「副本已漂移」。
- 回滚：改动集中在 `skillsStatus()` 一处分支与两行文案，还原该分支即回退到旧的统一 `copy-drift` 口径；测试与变更集同步还原。

## 执行结果

- 质量门：`pnpm typecheck` 通过；`pnpm build` 通过（ESM `dist/cli.js` 78.02 KB）；`pnpm test` 21 文件 / 177 用例全过（新增 1 例）；`toolkit tasks check` error 0 / warn 0；`toolkit tasks normalize` 0 问题。
- 实机核对：`toolkit skills status` 对 canonical 下 3 个上游安装器留下的未登记同名目录改报「同名冲突（非本包产物）」，汇总行为「共 3 处需处理：缺失 / 悬空 / 指向错误 / 副本漂移用 toolkit skills install 补齐，同名冲突用 toolkit skills install --force 覆盖」，与 `docs/cli.md` `status` 行、实现三者一致。

---

## 20260912-唐启云-优化skills自愈并新增开关

> 负责人：唐启云　状态：已完成　范围：toolkit　完成时间：2026-09-12 16:30

# 优化 skills 链接自愈并新增替换开关

> 来源链：延续 skills包内分发与全局安装（见 archive/202609/20260912.md）

## 背景

`skills.autoLink`（1.9.0 新增，默认 `true`）在每次执行 `toolkit` 命令时对状态文件 `~/.agents/.toolkit-skills.json` 记载的链接做补链与修链。排查发现四处问题：

1. **性能**：稳态开销实测 ≈ 32–35 ms/run（56 目标 × 3 链接），源于循环内重复判断真源存在性与逐条 `lstatSync`；单次虽小，但挂在所有 `toolkit` 命令的启动路径上。
2. **安全性**：对「状态文件登记为链接、现场却变成实体目录/普通文件」的条目会 `removeArtifact()`（`rm -rf`）后重建，存在误删用户目录的风险，且无配置可供收紧。
3. **一致性缺陷**：`RemoveResult.label` 为死字段（值恒等于 `entry.dir`，无消费者、无断言）；`skills status` 末尾汇总指引只覆盖「缺失 / 悬空 / 指向错误」，不覆盖「副本漂移 / 同名冲突」，与 7 态计数不一致。
4. **记账丢失**（既有瑕疵，与开关正交）：`autoLinkSkills()` 的 `links.filter` 用同一个 `return false` 承担两种语义——「技能已不在包内 → 剔除记录」与「重建失败 → 本应留档」。落盘闸门 `if (repaired > 0)` 是**跨目标全局计数**，故仅在同轮无处修复成功时「不写盘」才恰好成立；一旦同轮他处修好，失败条目会被静默写掉，与注释「留在状态里下次再试」矛盾。后果：`skills remove` 漏清理该条、`skills install` 对该槽位的冲突保护失效、`skills status` 丢失「本包登记」事实。

## 目标

1. autoLink 性能优化：对外行为完全不变，仅替换内部实现，本机 SSD 基线净增 < 20 ms/run（实测 ≈16–17 ms，旧实现 ≈32–35 ms）。
2. 新增开关 `skills.autoLinkReplaceForeign`（boolean，默认 `true`）：默认等价现状（实体目录/文件仍先删后建链）；关闭时遇实体目录/普通文件一律不动。
3. 消除上述一致性缺陷：删除 `RemoveResult.label`；`skills status` 汇总改分型指引。
4. 修复记账丢失：`return false` 只保留「技能已不在包内」一种语义；重建失败时保留记录，落回原注释意图。

## 实施方案

### 步骤 1：建档
本文件。

### 步骤 2：`src/skills.ts` — autoLink 实现优化 + 开关分支
- 开关读取：`getConfigSection("skills")?.autoLinkReplaceForeign === false` 进入非替换模式；总闸 `autoLink === false` 与 `CI` 短路逻辑不变。
- 拦截点唯一：`if (kind !== "absent") removeArtifact(dest)` 之前，复用现有 `classifyExisting()` 的 6 值（`absent` / `link-ok` / `wrong` / `dangling` / `dir` / `file`）。
- 语义矩阵：

| 现场 kind | 默认（开关 true） | 开关关闭 |
| --- | --- | --- |
| absent | 建链 | 建链 |
| link-ok | 不动 | 不动 |
| dangling | 删链重建 | 删链重建 |
| wrong | 删链重建 | 删链重建 |
| dir | rm -rf 后建链 | 一律不动 |
| file | 删除后建链 | 一律不动 |

- 关闭时 `dir` / `file` 保留现场、状态记录原样，且本条件不触发写盘（稳态零写盘不变）；可见性与修复入口不丢：`skills status` 仍报为「副本已漂移」或「同名冲突」，`skills install --force` 仍可强制转链。
- 记账修复：`catch { return false }` → `catch { return true }`。`return false` 只保留「技能已不在包内」一种语义；重建失败时保留记录，正好落回原注释意图，`repaired` 不增长、稳态零写盘不变。目标目录不可读（`readdirSync` 抛错）同样整条原样保留，不再连带丢弃（与记账修复同源：可读性问题不销毁记账）。
- 性能优化（行为不变）：现场条目一次 `readdirSync(dir, { withFileTypes: true })` 取得（Windows junction 经实测同样报为符号链接，分类口径不变）；真源存在性按「包内技能名集合」一次求交，去掉循环内逐条探活；真源已确认存在时跳过 `dest` 探活（指向存在真源的链接必然可解析，与旧 `existsSync(dest)` 结果等价）。
- 边界：不改 `installSkills()` 的冲突保护与 `--force` 语义，不改 `skillsStatus()` 分类口径，不改 `classifyExisting()` 对其余三处调用点的既有行为（新增参数默认值即旧行为）。

### 步骤 3：`src/__tests__/skills.test.ts` 补用例
- 开关关闭 + 现场为实体目录 → 目录完好、返回 0
- 开关关闭 + 现场为普通文件 → 文件完好、返回 0
- 开关关闭 + 悬空 / 指向错误链接 → 仍重建（确认只豁免 dir/file）
- 未配置开关 + 现场为实体目录 → 仍 rm 重建（默认行为回归保护）
- 保留既有 8 例（含 CI 跳过、`autoLink: false` 关闭、函数静默不写 stdout）
- 记账修复不另加注入式单测：跨平台可靠触发 `symlinkSync` 失败需污染包内真源或改目录权限，用例必然脆弱且与宿主环境耦合；以代码走查 + 任务档记录覆盖，不引入脆弱断言。

### 步骤 4：删除 `RemoveResult.label`
- 改 `src/skills.ts` 类型定义与 `removeSkills()` 赋值处。
- 依据：`exports` 仅 `"."` 且 `src/index.ts` 未导出 skills 域，无外部消费者；`printRemoveReport()` 从不读取该字段；测试无断言。

### 步骤 5：`src/cli.ts` status 汇总改分型指引
- 现文案「共 N 处需处理：执行 toolkit skills install 可补齐缺失、重建悬空或指向错误的链接」改为按态分型：缺失 / 悬空 / 指向错误 → `toolkit skills install`；副本漂移 / 同名冲突 → `toolkit skills install --force`。
- 计数口径与 `SKILL_PROBLEM_STATES` 严格一致，不新增、不遗漏态。

### 步骤 6：文档触达面逐篇核对
结论见「影响范围」。

### 步骤 7：变更集
- 新增字段并入现有未发布的 `.changeset/minor-skills-bundled-distribution.md`（1.9.0 尚未发版，字段属同一功能面）。
- 性能优化与 status 指引修复另建 patch 变更集。

### 步骤 8：质量门与复测
`pnpm build` → `pnpm test` → `node dist/cli.js tasks check`（error / warn 双零）→ grep 三方一致（代码 / help 文案 / 文档 / skills）→ `tasks normalize`（0 问题）；autoLink 性能复测（`Remove-Item Env:\CI` 后计时，目标 < 20 ms/run）。

## 影响范围

| 类型 | 文件 | 结论 |
| --- | --- | --- |
| 代码 | `src/skills.ts` | 触达：autoLink 实现与开关、记账修复、删死字段；`classifyExisting()` 新增可选参数但三处既有调用点行为不变 |
| 代码 | `src/cli.ts` | 触达：L176 status 汇总分型指引 |
| 代码 | `src/config.ts` | 触达：L8 顶部结构示例 skills 段补新字段（加载逻辑无改动：无 schema、段级合并，新增字段零成本） |
| 测试 | `src/__tests__/skills.test.ts` | 触达：新增 4 例开关用例 |
| 文档 | `README.md` | 无触达：L64-68 仅列 `toolkit skills install/status/remove` 命令名与查询作用，未描述 autoLink 及其替换语义 |
| 文档 | `docs/config.md` | 已同步：L29 结构示例、L127 字段表新增行、L132 作用边界新增条、L139 关闭示例 |
| 文档 | `docs/cli.md` | 已同步：L144 status 汇总改分型指引、L158 autoLink 行为行补开关 |
| 文档 | `docs/handbook.md` | 无触达：L102 仅列 7 态集合（本次未增删态）、L111 讲软链/副本同步机制，均未描述实体覆盖与汇总指引 |
| 文档 | `docs/guide.md` | 无触达：L121-122 命令清单、L245 升级说明，未描述 autoLink 行为 |
| 文档 | `docs/getting-started.md` | 无触达：L101/L107 安装命令与指引，未描述 autoLink |
| 文档 | `docs/faq.md` | 无触达：安装与内网离线场景，未描述 autoLink |
| 文档 | `docs/index.md` | 无触达：特性概览，未描述 autoLink |
| 文档 | `docs/api.md` | 无触达：仅列 config 加载函数签名，无 skills 段字段清单 |
| 文档 | `docs/ai-rules.md` | 无触达：L53-56/L82 只定安装与升级命令归属，未描述 autoLink 语义 |
| 文档 | `docs/changelog.md` | 发版时同步（`pnpm sync:changelog-doc`），非本次改动 |
| skills | `skills/README.md`、`skills/fxri-*/SKILL.md` | 无触达：仅出现 `skills install/status` 命令名（README.md:23-24、fxri-session-recap:131、fxri-release-changelog:59），未描述 autoLink 及其替换语义，version 不动 |
| 规范 | `.tasks/conventions.md` | 无触达：第 11 条讲分发机制（本次未改）、第 12 条讲 stdout/stderr 分流（本次不新增输出通道） |
| 变更集 | `.changeset/minor-skills-bundled-distribution.md`、`.changeset/patch-skills-autolink-tuning.md` | 已建：新字段并入未发布的 minor；性能优化 / status 指引 / 记账修复另建 patch |

## 验证与回滚

- 验证：`pnpm build`；`pnpm test`（覆盖率 statements 80 / branches 65 / functions 80 / lines 82）；`node dist/cli.js tasks check` 双零；实机构造实体目录/普通文件槽位，分别验证默认与开关关闭两态；`node dist/cli.js skills status` 走查分型指引；性能复测 < 20 ms/run；记账修复走查——确认 `return false` 仅剩「技能已不在包内」一条路径。
- 已实测（本机 Windows / SSD，57 目标 / 171 链接，`Remove-Item Env:\CI` 后计时，三轮稳定）：autoLink 开 ≈103–106 ms/run、关 ≈87–88 ms/run，净增 ≈16–17 ms/run；旧实现净增 ≈32–35 ms/run。
- 性能口径：开销为文件系统调用量级（`57 次 readdirSync + 171 次 readlinkSync`），与 CPU 主频基本无关，按「目标目录数 + 链接数」线性放大；低配机 / 机械盘 / 网络家目录 / 杀软实时扫描环境按比例上升，同时 node 启动基线同比例变慢，占比基本稳定；`skills.autoLink: false` 可整体关闭。
- 回滚：改动集中在 `src/skills.ts` / `src/cli.ts` / 测试与文档，`git revert` 单提交即可；开关默认 `true` 保证既有用户行为不变，也可先设 `skills.autoLinkReplaceForeign: false` 临时收紧而不回滚代码。

---

## 20260912-唐启云-修复提示污染标准输出

> 负责人：唐启云　状态：已完成　范围：toolkit　完成时间：2026-09-12 15:32

# 修复提示污染标准输出

延续 skills包内分发与全局安装（见 archive/202609/20260912.md）

## 背景

对 1.9.0 新增的 skills 包内分发能力做四方复核（功能 / 三方一致 / 不强制装工具 / autoLink 开销）时，实测确证两处真实缺陷：

- `src/cli.ts` 的 autoLink 自愈提示走 `console.log`；
- `src/update-check.ts` 的升级提示走 `console.log`。

`docs/cli.md` 承诺 `--format json` 输出到 stdout，两处提示都会追加在 JSON 之后，破坏机器可读契约。实测证据：

```
STDOUT-HEAD: "{\n  \"schemaVersion\": 1, ... \"items\": []\n}\n⬆️ 发现新版本 9.9.9..."
parses? NO -> Unexpected non-whitespace character after JSON at position 114
STDERR: ""
```

注意这不是「代码与文档不一致」：`docs/config.md` 已把「标准输出会打印一行」写进规格，属**代码与文档口径一致、但二者都与机器可读契约冲突**，需代码 + 测试 + 文档三处同批修正。

同批对齐一处口径瑕疵：`src/update-check.ts` 的 `readCache()` 未剥 BOM，与 `src/config.ts`、`src/skills.ts` 的既有做法不一致（带 BOM 时判为缓存损坏并重新联网，属自愈型，不报错）。

## 实施方案

1. `src/cli.ts`：autoLink 自愈提示 `console.log` → `console.error`；
2. `src/update-check.ts`：`notify()` 的 `console.log` → `console.error`，同步修正顶部与函数注释中的口径描述；
3. `src/update-check.ts`：`readCache()` 的 `JSON.parse` 前补 `.replace(/^\uFEFF/, "")`，与 `config.ts` / `skills.ts` 对齐；
4. `src/__tests__/update-check.test.ts`：`captureLog()` 改为捕获 `console.error`，核对其余断言；
5. `src/__tests__/skills.test.ts`：补断言——`autoLinkSkills()` 自身只返回计数、不写 stdout（提示位于 `src/cli.ts` 的 `main()`，本仓库无 CLI 级集成测试且 `cli.ts` 顶层即执行，无法导入断言输出流，故以「函数静默」用例守住同类缺陷，CLI 层输出流由实测验收覆盖）；
6. 文档同步：`docs/config.md`（updateCheck 提示输出流、skills 护栏条目的「标准输出」措辞）、`docs/cli.md` 行为细节；复查 `README.md`、`docs/faq.md`、`docs/ai-rules.md`、`docs/guide.md`、`docs/handbook.md` 中提及提示的措辞；
7. 补充 `.changeset/`（patch：`patch-stream-separation.md`）与触达的 skills 版本号核对（本次不改 skill 流程，三个 SKILL.md 无触达、版本号不动）。

## 影响范围

- 仅输出流归属变化，不改任何能力语义与命令行为；
- `console.error` 跨平台可用，两处均为命令前提示或 fire-and-forget，不影响退出码；
- 不涉及 skills 内容与分发逻辑，三个 SKILL.md 无触达（版本号 1.1.3 / 1.0.7 / 1.1.1 不动）。

触达面逐篇核对（`docs/` 按 AGENTS.md 清单）：

| 文档 | 结论 |
| --- | --- |
| `docs/cli.md` | 已同步（`--format json` 行补「诊断与提示信息一律走 stderr」） |
| `docs/config.md` | 已同步（updateCheck 段写明 stderr；skills 护栏条目「标准输出」改「标准错误」） |
| `README.md` | 无触达（只述能力概览，未提提示输出流） |
| `docs/getting-started.md` | 无触达（同上） |
| `docs/handbook.md` | 无触达（grep 无「已自动修复 / 升级提示 / stdout」命中） |
| `docs/guide.md` | 无触达（「自动修复」仅指 `tasks normalize --fix`，与本次无关） |
| `docs/faq.md` | 无触达（未提提示输出流） |
| `docs/api.md` | 无触达（不导出 update-check / autoLink 相关 API） |
| `docs/ai-rules.md` | 无触达（仅提「看到版本提示提醒用户升级」，与输出流无关） |
| `skills/` | 无触达（不描述输出流；版本号不动） |

## 验证方式

- `pnpm build` 后重跑两组实测：删 junction 后 `tasks --format json` 的 stdout 必须能 `JSON.parse` 成功；写入无 BOM 缓存后 stdout 仍为纯 JSON、提示出现在 stderr；
- `pnpm test` 全通过（含新增断言）；
- `node dist/cli.js tasks check` error / warn 双零；
- grep 三方一致（注意 dist 中文被 esbuild 转义为 `\uXXXX`，须用 ASCII 关键字检索）；
- `node dist/cli.js tasks normalize` 0 问题。

## 执行结果

改动清单：

- `src/cli.ts`：autoLink 自愈提示 `console.log` → `console.error`，补注释说明 stdout 为机器可读专用通道；
- `src/update-check.ts`：`notify()` 改 `console.error`；`readCache()` 解析前补剥 BOM；
- `src/__tests__/update-check.test.ts`：捕获器改为同时捕获 stderr / stdout，7 个用例断言升级为双流断言，新增「缓存带 BOM 仍按命中处理」用例；
- `src/__tests__/skills.test.ts`：新增「自愈只返回计数、不写 stdout」用例；
- 文档：`docs/cli.md`（`--format json` 行补输出流口径）、`docs/config.md`（updateCheck 段与 skills 护栏条目）；
- 变更集：`.changeset/patch-stream-separation.md`（patch）。

验证证据（`pnpm build` 后重跑，home 与 tmp 均注入独立临时目录，不触碰真实用户环境）：

```
exitCode: 0
stdout parses? true
stdout head: "{\n  \"schemaVersion\": 1,\n  \"sum"
stderr has autoLink notice? true
stderr has update notice? true
repair applied? true
stderr raw: "已自动修复 1 个技能链接（...）\n⬆️ 发现新版本 99.0.0（当前 1.8.3）：..."
```

质量门：

- `pnpm build` 成功（ESM `dist/cli.js` 77.69 KB / `dist/index.js` 44.50 KB）；
- `pnpm test` 21 文件 / 172 用例全通过（改前 170，本轮 +1 BOM 用例 +1 静默用例）；
- `node dist/cli.js tasks check` error 0 / warn 0；
- `node dist/cli.js tasks normalize` 0 问题；
- grep 三方一致：`src/cli.ts:588`、`src/update-check.ts:86` 均 `console.error`（dist 内中文被 esbuild 转义，须以 ASCII 关键字核对，行为已由上述隔离实测确认）。

---

## 20260912-唐启云-skills包内分发与全局安装

> 负责人：唐启云　状态：已完成　范围：toolkit　完成时间：2026-09-12 15:02

# skills 包内分发与全局安装能力

## 背景

现状装/升级需两条**互不相关**的命令：`pnpm add -g @fxri/toolkit`（CLI，走 npm registry）+ `pnpm dlx skills add fxri-net/toolkit --global`（skills，走上游 vercel-labs/skills 从 GitHub 拉取）。两条供应链版本无关联，易漂移，心智负担高。

上游另有两条未修缺陷直接落在本场景：

- issue #537（open）：全局安装时 canonical 副本写入成功，但 agent 专属目录（`~/.cursor/skills` 等）**不被创建**，`skills list -a <agent> -g` 显示 `Agents: not linked`，需用户手动 mkdir 或重装；
- issue #693（open）：Claude Code 全局不读 canonical，只读 `~/.claude/skills`。

本任务把 skills 改为**从包内 `skills/` 目录分发**（`package.json` 的 `files` 已含 `skills`），新增 `toolkit skills` 命令域，使**升级阶段一条命令**即可，并正面绕开上述两条缺陷。

已核验事实（来源可查）：

- Agent Skills 规范只定义技能目录结构与 frontmatter 字段，**不规定安装位置**（agentskills.io/specification）；
- `~/.agents/skills/` 是上游安装器**自设的 canonical store**，非标准；上游表中 7 家 agent 共读该目录；
- 上游维护 75+ agent 的全局/项目路径表且持续新增 → 内置表必然过期，需 `--dir` 兜底；
- 上游支持**本地路径源**安装（`npx skills add ./path`），可作为表外 agent 的委托路径；
- pnpm v10 起**默认禁止依赖执行 postinstall** → 依赖 postinstall 自动装 skills 不可靠；
- Windows 上 `fs.symlink(target, link, 'junction')` 创建 junction **免管理员、免开发者模式**，POSIX 忽略 `type`；实测 junction **可跨本地卷**（本机 `C:\Users\<user>\__jtest_cross__` → `D:\...\skills\fxri-plan-to-task` 创建成功且可读，`isLink=true`），失败场景落在网络路径（UNC）、非 NTFS 卷、权限不足等，而非跨盘。

## 实施方案

**命令面**（新增 `skills` 域，仿 `init` 的 commander 写法）：

- `toolkit skills install [--copy] [--dir <path>]... [--dry-run] [--force]`
- `toolkit skills status`
- `toolkit skills remove [--dry-run]`
- `toolkit skills path [--json]`

**真源与目标**：真源 = 包内 `<包根>/skills/<name>/`（含 `SKILL.md` 才计为技能）。所有目标目录**一律链接到真源**——与上游「canonical 存真身」的做法不同，本方案真源唯一、无链中链、`pnpm add -g` 升级后所有链接自动跟随。

目标分三层：

1. 主目标 `~/.agents/skills/`（上游 canonical，7 家 agent 共读）；
2. 附加目标 = 内置快照表中**已安装**的各 agent 全局技能目录（判据：技能目录的父目录即该 agent 配置目录存在；不存在则只报告不创建，避免在用户机器上凭空造目录）；
3. `--dir <path>`（可多次）任意目标，兜底表外新 agent，无需等 toolkit 升级。

**链接与降级**：默认软链（POSIX `symlink`、Windows `symlink(..., 'junction')`）；**链接创建失败时自动降级 copy**（实测 junction 可跨本地卷，故触发条件落在网络路径 / 非 NTFS 卷 / 权限不足等异常，不预设「跨卷」），并打印 ⚠️ 警告（原因 / 影响 / 修复命令），不静默；`--copy` 强制副本。幂等：已存在且指向正确 → 跳过并计入报告；指向错误或**悬空** → 重建。

⚠️ 升级跟随的诚实边界：Node 解析主模块时会对软链取 realpath，pnpm 全局安装下包路径含版本段（`.pnpm/@fxri+toolkit@x.y.z/...`），升级后旧链接可能**悬空**。因此软链并非「一定自动跟随」，而是由 `autoLink`（默认 true）在下次运行任意 `toolkit` 命令时自动**修链**；`skills status` 也会把悬空/指向错误显式报告出来。

**状态文件** `~/.agents/.toolkit-skills.json`（与上游 `.skill-lock.json` **分开命名**，避免冲突）：记录本包写入的产物、各目标模式（link/copy）、写入时间。双重用途——`status` 据此报告副本漂移并提示重跑 install；`remove` 据此**精确识别本包产物**，落实「只删本包写入，绝不碰用户自装技能」。

**配置** `skills.autoLink`（默认 `true`）：作用边界为**补链 + 修链**——仅对已存在目录补链、CI 环境跳过、失败静默降级并留一行日志；**不含**首次自动安装、**不含**自动升级（软链模式下版本同步由链接天然保证，只有 copy 副本漂移，仅提示不自动覆盖）。

**包根定位**：用 CLI 自身模块位置（`import.meta.url`）解析，不依赖 `pnpm root -g` 之类猜路径 → npm / pnpm / yarn / bun 装的都通用。

**不做项**：postinstall 自动安装（pnpm 10 拦截 + 用户已否决）、Go 二进制化（收益极低且需改造 changesets 调用链）、全局规则强制安装（与 ai-rules「AI 不得自行执行全局安装/升级」冲突，降级为检测 → 给命令 → 用户确认后执行）、项目级安装（本版只做全局）、内置 `--via` 委托上游（改由 `skills path` + 用户手工 `pnpm dlx skills add "$(toolkit skills path)" -g` 覆盖表外 agent）。

**安装/升级链路**（诚实边界，不夸大）：

| 阶段 | 命令 | 条数 |
| --- | --- | --- |
| 首次 | `pnpm add -g @fxri/toolkit && toolkit skills install` | 一行两条 |
| 升级 | `pnpm add -g @fxri/toolkit` | 一条（软链自动跟随） |
| 升级（含 copy 目标） | 上述 + `toolkit skills install` | 两条 |

首次仍需两步，不做 postinstall 的前提下无解；本方案消除的是**每次升级**的那条，即痛点所在。

## 影响范围

- **能力自身**：新增 `skills` 命令域（4 子命令）+ 配置段 `skills.autoLink` + 状态文件；`init` / `tasks` / `changelog` 现有行为零改动。质量门「代码实际行为 / 单元测试 / CLI help 文案」三者一致。
- **docs 逐篇触达核对**（按 AGENTS 质量门第 2 条，方案确认时走查，每篇标注动作）：
  - `README.md`：触达（skills 安装节、安装方式节、能力表）→ 本任务同步
  - `docs/getting-started.md`：触达（三种安装方式 +「关于 AI 技能包」节）→ 本任务同步
  - `docs/handbook.md`：触达（「六、升级」命令与三步检查）→ 本任务同步，补卸载顺序与悬空链接说明
  - `docs/guide.md`：触达（「AI 技能包 skills」节、「升级与版本兼容」节）→ 本任务同步
  - `docs/faq.md`：触达（「内网或离线环境怎么装」需改口径）→ 本任务同步，新增「卸载工具时 skills 怎么办」条目
  - `docs/cli.md`：触达（新增 `skills` 章节）→ 本任务同步
  - `docs/config.md`：触达（新增 `skills` 段：`autoLink` 默认值与护栏）→ 本任务同步
  - `docs/ai-rules.md`：触达（「环境与版本」命令块）→ 本任务同步，保持薄壳不复述可变细节
  - `docs/index.md`：触达（首页亮点「AI 技能包」措辞）→ 本任务同步
  - `docs/api.md`：无触达（`src/index.ts` 不导出 skills 域，非编程 API）
  - `docs/.vitepress/config.mts`：无触达（不新增独立页，`skills` 归入 cli/config 既有页）
  - `docs/changelog.md`：无触达（发版时 `sync:changelog-doc` 镜像，不手改）
  - `SPEC.md`：无触达（grep 零 skills 安装描述）
  - `.github/`：无触达（无 skills 相关配置）
- **skills 逐篇触达核对**：
  - `skills/fxri-plan-to-task/SKILL.md`：触达（「可选加速」节补 `toolkit skills install|status`）→ 同步，`metadata.version` +patch
  - `skills/fxri-session-recap/SKILL.md`：触达（同「可选加速」节）→ 同步，`metadata.version` +patch
  - `skills/fxri-release-changelog/SKILL.md`：触达（同「可选加速」节）→ 同步，`metadata.version` +patch
  - `skills/README.md`：触达（安装方式以内置命令为首选；删除第 30 行「已知上游行为」条目——该缺陷已被本方案正面修掉）
- **conventions 同步**：本任务若修改 AGENTS / ai-rules / skills 条文，须同步检查 `.tasks/conventions.md` 溯源索引对应行（当前语义是否需更新）；语义变更先在「演进记录」留痕。
- **版本**：minor → 1.9.0。先例：1.7.0 新增 `toolkit init` 命令归入「✨ 新增功能」走 minor。`.changeset/` 当前无待消费集，本任务新建 1 条 minor 变更集。发版链路（消费/构建/提交/标签/发布/推送）独立执行，发布与推送需用户明确。

## 验证与回滚

- **实施前实测**（结论进「执行结果」节）：Trae / Trae CN 的真实技能加载目录；Windows junction 同卷与跨卷（模拟跨卷或直接验证降级分支）的实际行为。
- **验证**：`pnpm build` → `node dist/cli.js skills --help|path|status|install --dry-run` 四项人工核对 → 真机 `skills install` 后开新会话确认 Trae 读到 fxri-* 技能 → `node dist/cli.js skills status` 报告正确 → `skills remove --dry-run` 预览后执行并确认无残留 → `toolkit tasks check` error/warn 双零 → `pnpm test` 全量通过 → grep 复核文档与 skills 措辞落地（三方一致）。
- **回滚**：改动集中在新文件 `src/skills.ts` + 三个小改文件 + 文档；已写入用户 home 的链接可用 `node dist/cli.js skills remove` 清除（状态文件同删）；单提交 `git revert` 即可恢复，任务区与既有能力零影响。
- **时间取证**：`completed` 按四级时间源当场打点。

## 执行结果

**落地内容**：新增 `src/skills.ts`（技能枚举 / 三层目标解析 / 链接与副本 / 状态比对 / 卸载 / 状态文件）、`src/__tests__/skills.test.ts`（35 用例）；改 `src/cli.ts`（`skills` 命令域四子命令 + 命令执行前调用 `autoLinkSkills()`）、`src/config.ts`（`skills.autoLink` 段）、`src/update-check.ts`（升级提示语）；文档同步 `README.md`、`skills/README.md`、`docs/`（getting-started / handbook / guide / faq / cli / config / ai-rules / index）、三个 `SKILL.md`（`metadata.version` 各 +patch：1.1.3 / 1.1.2 / 1.0.7）、`AGENTS.md` 质量门第 5 条与 `docs/ai-rules.md` 旧口径（`pnpm dlx skills update` → 技能随包分发、与 CLI 同源）；新增 `.changeset/minor-skills-bundled-distribution.md`（minor）。

**方案确认时列的触达面逐篇核对结论全部落地**：`docs/api.md` 无触达（`src/index.ts` 不导出 skills 域）、`docs/.vitepress/config.mts` 无触达、`docs/changelog.md` 无触达（发版时镜像）、`SPEC.md` 无触达、`.github/` 无触达——五处零改动，与方案结论一致。

**验证证据**：

| 项 | 结果 |
| --- | --- |
| `pnpm build` | 通过（ESM `dist/cli.js` 77.67 KB / `dist/index.js` 44.50 KB，CJS 与 DTS 全部生成） |
| `skills --help` / `path` / `path --json` / `status` | 四项人工核对通过，四子命令 description 与 `docs/cli.md` 行为表逐字一致 |
| 隔离假 home 生命周期实测 | install（只对已存在 agent 目录建链，跳过未安装 agent 不凭空造目录）→ 状态文件结构正确 → 重跑幂等 → `--copy` 写副本 → 改副本后 status 报「副本已漂移」→ `install --dry-run` 对同名非本包产物报「跳过」→ `remove --dry-run` 预演正确 → `remove` 后链接与状态文件清除且真源文件数不变（3）→ 有漂移副本时 remove 保留并提示人工确认 |
| `autoLinkSkills` 自愈 | 清除 `CI` 后自动补链成功并打印修复条数；`CI` 为真时静默跳过（护栏按设计生效） |
| 真机 `skills install` | 通过：重建 168 处 agent 链接 + 新建 6 处（GitHub Copilot、Kilo Code），`skills status` 全部「软链正常」；经 `~/.trae-cn/skills/fxri-plan-to-task/SKILL.md` 读到包内最新 `version: "1.1.3"`，证明链接交付最新内容 |
| 真机 `skills remove --dry-run` | 通过：正确列出本包写入的 57 个 agent 目录 × 3 技能，canonical 下 3 个上游安装器副本未列入（非本包产物） |
| `toolkit tasks check` | error 0 / warn 0 |
| `pnpm test` | 21 文件 170 用例全通过（含 `skills.test.ts` 35 例） |
| grep 三方一致复核 | 代码 help 文案 / 测试 / 文档措辞一致；残留 `pnpm dlx skills add` 命中均为刻意的「方式二：上游安装器」语境或历史记录 |

**诚实边界与遗留**：

- 真机 `install` 写入的链接指向本仓库开发路径（因以 `node dist/cli.js` 直调运行，包根即仓库根）；发布后经全局安装再跑一次 `skills install` 即指向全局包路径，`autoLink` 亦会在下次运行任意 `toolkit` 命令时修链。
- canonical `~/.agents/skills/` 下 3 个上游安装器装的副本仍为「副本已漂移」，按「不覆盖用户既有非本包产物」原则自动跳过、仅报告，不合并、不删除。
- 真机 `remove` 未实际执行：假 home 已完整覆盖「删链 + 删状态文件 + 真源无损 + 漂移副本保护」全链路，真机执行只会清掉刚建成的工作现场，故以 `--dry-run` 交付证据。
- 表外新 agent 由 `toolkit skills path` + 上游安装器手工委托，不内置 `--via`。
- `remove` 报告用目标绝对路径而非 agent 中文名（与 install/status 命名口径不一致），非阻塞，留待后续统一。

**时间取证**：`completed` 为验收完成当场打点（四级时间源第 0 级）。
