# 20260905 归档

> 本文件由 `toolkit tasks archive` 自动生成。

## 20260905-唐启云-现有skills复查与站点部署配置

> 负责人：唐启云　状态：已完成　范围：toolkit　完成时间：2026-09-05 09:30

# 现有 skills 复查与站点部署配置

## 实施方案

### A. skills 复查（fxri-plan-to-task、fxri-release-changelog）

- 「可选加速」节工具探测顺序（npx → 全局）写醒目，工具缺失时行为说清楚
- 与 docs/ 内容去重收敛，单点互链；与 cli/config 真实能力一致性核对
- 自查清单补「归档提交前确认 .tasks 与归档文件在提交范围内」（用户反馈）
- 两 SKILL.md metadata.version 递增

### B. VitePress 站点

- devDep 引入 vitepress（执行时取 Node 20 兼容最新稳定版）
- `docs/.vitepress/config.mts`：zh-CN 主题、sidebar/nav、本地搜索、`base: process.env.VITEPRESS_BASE || '/toolkit/'`
- package.json scripts：docs:dev / docs:build / docs:preview
- `.gitignore`：docs/.vitepress/dist、cache

### C. 多平台部署

- `.github/workflows/deploy-docs.yml`：main 分支 docs/** 触发，官方 deploy-pages action
- `.gitlab-ci.yml`：pages job，dist 挪 public/ 产出 artifacts，VITEPRESS_BASE 注入
- Gitee：手动构建推 docs-pages 分支步骤写入 guide.md（Gitee Pages 无 CI 自动构建）

### D. CI

- 现有 check job 追加 docs:build（逐次全跑，死链检查全覆盖，已确认）

## 影响范围

- skills/ 下 2 技能、package.json、.gitignore、docs/.vitepress/、两个 CI 文件
- vitepress 仅 devDep，不影响 npm 包产物

## 验证方式

- docs:build 本地通过、死链零告警
- workflow YAML 语法校验
- skills 与 docs 交叉链接可达

## 完成说明

### A. skills 复查（已完成）

- 三个 SKILL.md（plan-to-task / release-changelog / session-recap，session-recap 为后建一并纳入）「可选加速」节统一改为 ⚠️ 醒目探测顺序：先 `pnpm exec toolkit --help`（npm 项目等价 `npx toolkit`）→ 失败 `toolkit --help`（全局）→ 都不可用即未安装，手工继续不安装不阻塞；探测成功用对应前缀执行
- 命令清单与 cli.md 逐项核对一致：tasks / check / archive（--dry-run）/ normalize（--fix）/ --dir、changelog / version / format / --lang
- task-spec.md 自查清单补「归档提交前确认 .tasks 全部改动纳入提交范围，与代码变更同批」
- metadata.version 递增：fxri-plan-to-task 1.0.2、fxri-release-changelog 1.0.2、fxri-session-recap 1.0.1
- 与 docs 去重收敛：技能自包含原则维持（不引用 docs），docs 与 skills 各自单点（安装细节在 skills/README.md，docs 侧为概念答疑视角）；无内容漂移

### B. VitePress 站点（已完成）

- devDep vitepress 1.6.4（Node 20 兼容最新稳定版）
- `docs/.vitepress/config.mts`：zh-CN、nav（指南/参考/FAQ/AI 全局规则）、三组 sidebar、本地搜索中文界面文案、outline/docFooter/lastUpdated 中文化、socialLinks 指仓库；`base` 默认 `/toolkit/`，`VITEPRESS_BASE` 环境变量可覆盖
- package.json scripts：docs:dev / docs:build / docs:preview
- `.gitignore`：docs/.vitepress/dist、docs/.vitepress/cache
- 验证：`pnpm docs:build` 通过，死链零告警

### C. 多平台部署（已完成）

- `.github/workflows/deploy-docs.yml`：push main 且 docs/** 等路径变更触发；permissions 按 Pages 官方最小集；concurrency 取消旧部署；build（upload-pages-artifact）+ deploy（deploy-pages@v4）双 job
- `.gitlab-ci.yml`：pages job（node:20 镜像 + corepack enable），`VITEPRESS_BASE=/toolkit/` 注入构建，dist 挪 `public/` 产出 artifacts；仅默认分支 docs 相关变更触发
- Gitee 手动步骤写入 guide.md 部署节：本地构建（含 Windows PowerShell 环境变量写法）→ dist 内 git init -b docs-pages → 强推专用分支 → Gitee Pages 选分支构建
- 验证：三份 YAML（deploy-docs / gitlab-ci / ci）经 pnpm dlx js-yaml 解析全部通过

### D. CI（已完成）

- ci.yml check job 在「构建」与「CLI 冒烟」之间追加「文档站构建（死链检查）」，逐次全跑

---

## 20260905-唐启云-新增fxri-session-recap会话归档技能

> 负责人：唐启云　状态：已完成　范围：toolkit　完成时间：2026-09-05 01:50

# 新增 fxri-session-recap 会话归档技能

## 背景

用户会话结束后上下文散失，跨会话无法延续。按已确认的场景 A 设计：全工具通用、零依赖。

## 实施方案

- `skills/fxri-session-recap/SKILL.md`：
  - 「归档本次会话」：沉淀当前会话 → 结论/决策写进任务文件（建档或更新）→ 未完事项拆独立任务 → 走 fxri-plan-to-task 归档流程
  - 「恢复上下文」：读 active 任务 + 最新归档 → 重建现场
  - 何时使用/不使用；跨会话能力边界说明（其他会话全文不可直读，依赖归档习惯；Claude Code 可直读转录为高级路径）
  - 主干 200 行内，细节下沉 references/
- `skills/README.md` 索引更新

## 影响范围

- 仅 skills/ 下文档（随 npm 包分发）

## 验证方式

- 与 fxri-plan-to-task 流程衔接一致（引用 task-spec.md 不重复定义）
- frontmatter 合规：name fxri- 前缀、description 含触发词与反向排除

## 完成说明

- SKILL.md 落盘：归档模式六步（回顾盘点 → 逐条落盘 → 状态收尾 → 归档 → 归档提交同批 → 回报确认）+ 恢复模式四步（active + 最新归档 → 现场摘要 → 全程只读）；「能力边界」节写明跨会话共性约束与 Claude Code 直读转录的非通用性
- 用户追加的三条评估全部纳入：①「只记功能不记闲聊」精化为——与工作产出无关的闲聊不记，但**对话中的决策及理由必须记**、用户主动要求记录的照记；②「装好 skills 规则就全带上，不需要参考文件」成立——技能自包含（SKILL.md + references/assets 随技能目录分发），仅在「技能独立安装且项目无 CLI」时兜底引用仓库根 SPEC.md；③「配好全局规则就完全不用管 / 零负担 AI 自主」——日常成立，保留三个用户出手时刻（首次安装 / 升级开新会话 / 工具不可用兜底），将落在 getting-started「AI 用户」节与 faq（后续文档任务批次执行）
- frontmatter 合规：name fxri- 前缀、description 含触发词与反向排除、metadata.version 1.0.0、主干 < 100 行、规范细节引用 fxri-plan-to-task 的 task-spec.md 不重复定义
- skills/README.md 技能列表补第三行
- 校验：node dist/cli.js tasks check 通过（error 0；依赖已归档 warn 属正常）

---

## 20260905-唐启云-编写docs八篇文档体系

> 负责人：唐启云　状态：已完成　范围：toolkit　完成时间：2026-09-05 01:09

# 编写 docs 八篇文档体系

## 背景

现有 README/SPEC 无法支撑新用户自助上手。按需求拆分为双形态内容（GitHub 直读 + VitePress 站点同源），能力前置、链接跳详情、去重收敛。

## 实施方案

docs/ 下新建八篇：

1. `index.md` 站点首页：是什么/解决什么痛点/能力矩阵/文档索引
2. `getting-started.md` 新手指南：30 秒上手、零基础术语、安装三方式对比（项目 devDep / 全局 / 独立 pnpm，含 nvm/corepack 切版本陷阱）、能力前置链接
3. `guide.md` 完整攻略：方案落盘（任务如何产生）、会话归档、Git 纳管范围（.tasks 整体入库、.toolkitrc.json 建议提交、.archive.lock 忽略）、项目级强制激活模板、三平台部署发布、多语言 CHANGELOG
4. `cli.md` CLI 参考：tasks/changelog/init 全命令、参数、默认值、退出码、help footer 说明
5. `api.md` Node API 参考：按 src/index.ts 实际导出编写，纯 CLI 则并入 cli.md（执行时确认）
6. `config.md` .toolkitrc.json 参考：字段枚举、脱敏规则、scope 校验
7. `faq.md` FAQ：工具与 skills 的区别、.tasks 入库范围、node 切版本全局包丢失、只在公司项目激活、会话归档边界等（匿名化，不留真实人名）
8. `ai-rules.md` 推荐全局规则：源自作者自用全局规则三层转化（通用 AI 协作规则原样 / toolkit 专属工作流视角转换 / 安装升级校验命令直接可用），去标识处理；升级后开新会话 ⚠️ 强调

补充（用户确认，防止遗漏）：

- **工具与 skills 的关系结论必须显式成章**：skills 独立可用（零依赖纯 Markdown，装了即可跑通工作流）；工具是可选加速（自动建档/校验/归档）；推荐都装但不强制。落在 getting-started.md（入门视角）与 faq.md（答疑视角）两处
- **零基础术语科普（不懂 AI 的用户也看得懂）**：真实用户反馈「skills 是啥我都还没怎么研究过」——getting-started.md 增设「几个术语，先说人话」小节（AI 编程助手/agent/skills/MCP/CLI 用类比解释，不堆缩写）；faq.md 增设「skills 是什么？和插件/脚本有什么区别？」「我不用 AI，这个工具对我有用吗？」条目；全文避免未解释的缩写首次出现即使用
- **文档美化**：八篇统一页面结构模板（标题 → 一句话定位 → 「解决什么问题」→ 正文 → 「相关页面」互链）；index.md 用 hero 首屏布局；表格/代码块/⚠️ 注意事项风格统一；README 入口页与 SPEC.md 同批美化

同步：SPEC.md 瘦身为规范摘要+站点链接；各能力页开头统一「解决什么问题」小节。

## 影响范围

- 新增 docs/ 八篇；SPEC.md 瘦身；README 入口页（独立任务）
- npm 包 files 白名单暂不含 docs/，不影响产物

## 验证方式

- 相对链接可达（站点构建死链检查兜底）
- 与真实能力一致性核对：cli.md 逐命令对 src/cli.ts、config.md 逐字段对 .toolkitrc.json 解析代码

## 完成说明

- docs/ 八篇全部落盘：index（痛点 + 能力矩阵 + 文档索引）/ getting-started（术语科普 + 安装三方式对比 + 30 秒上手）/ guide（工作流 + 规范速查 + Git 纳管 + 项目级激活 + 会话归档 + 脱敏 + 多语言 CHANGELOG + 三平台部署 + 升级）/ cli（全命令字典 + 退出码）/ api（按 src/index.ts 导出同源）/ config（四段字段枚举）/ faq（四主题 15 条，匿名化）/ ai-rules（三层转化规则全文）
- 用户中途补充全部纳入：工具与 skills 关系显式成章（getting-started + faq）；零基础术语科普「几个术语，先说人话」（真实用户反馈「skills 是啥」）；美化统一为「标题 → 一句话定位 → 解决什么问题 → 正文 → 相关页面」结构
- init / fxri-session-recap / 升级检查等 1.7.0 新能力在文档中带版本标注预写，P2 实现后由 P3 验收矩阵核对一致性
- 仓库根文件（README/SPEC/skills）统一用 GitHub 绝对链接，docs 内部用相对链接，为 VitePress 死链检查铺路
- 相对链接可达性待 p1-vp 站点构建死链检查兜底；CLI 参数已逐项对 src/cli.ts 核对
- 归档后补记（用户追加需求，已完成）：**包管理器适配，主推 pnpm**——getting-started.md 安装章节 pnpm 命令前置，npm 次选，yarn 标注 v2+ 需先启用 global，补 `pnpm dlx`/`bunx` 临时执行；全局安装差异写明（npm bin 硬链于 Node 目录、pnpm 走 pnpm home）；安装方式对比表补包管理器行；faq.md 升级条目、ai-rules.md 配套安装、guide.md 升级节同步多包管理器命令。评估结论：工具本体零包管理器耦合，任何可装 npm 包的包管理器均可用，差异仅在全局 bin 落点
- 归档后补记（用户追加需求，已完成）：**全文档 npx/npm 命令转 pnpm 系**——命令映射三分法：全局装 `pnpm i -g`、项目内已装 `pnpm exec toolkit`、未装临时执行 `pnpm dlx`（弃用 pnpx：pnpm v7 起它仅是 pnpm dlx 的弃用别名）；涉及 README、getting-started、faq、guide、cli、ai-rules、skills/README 七文件；cli.md 改用中性直调 `toolkit <command>` 并在全局节写明三种调用方式；npm 用户等价命令以行内注释保留（npx 仅出现在等价说明、skills 官方安装器写法、上游 changesets 用法与历史记录中）；用户全局规则底稿已由用户自行改为 pnpm 系，未重复修改
- 归档后补记（用户追加需求，已完成）：**多包管理器并列时 pnpm 排第一**——getting-started.md 方式三命令顺序调整为 pnpm dlx / npx / bunx；skills/README.md 安装命令块改为 pnpm dlx 优先、npm 等价命令注释保留；全仓核查其余并列处（方式二全局安装、README 安装节、ai-rules 配套安装、faq 升级与 nvm 条目、guide 升级节、AGENTS.md 模板）均已 pnpm 在前，无遗漏
- 归档后补记（用户追加需求，已完成）：**AI 协作体验三条评估落档**——①「配好全局规则就完全不用管 / 零负担 AI 自主」：日常零操作成立，保留三个用户出手时刻（首次安装 / 升级开新会话 / 工具不可用兜底），回报确认是安全设计非操作负担，落在 getting-started「AI 用户」节 + faq「配好全局规则后」条目；②「只记录功能，纯对话不记录」：精化为闲聊不记、决策及理由必须记、用户主动要求照记，落在 getting-started「记录范围」+ faq「记什么不记什么」条目 + fxri-session-recap 第 1 步；③「装好 skills 规则就全带上，不需要参考文件」：技能自包含成立，仅独立安装且项目无 SPEC.md 时兜底参照，落在 faq「skills 需要额外的仓库参考文件吗」条目 + fxri-session-recap 第 2 步兜底说明
- 归档后补记（用户追加需求，已完成）：**适用角色评估落档**——基于真实能力核实（导入中文列别名 / 过滤视图 / 导出 / depends_on 闭环校验 / 脱敏规则）评估：开发（全功能）、产品经理（Excel 按常见中文列名整理 → tasks --import 生成任务文件）、项目经理（过滤视图 + 导出汇报 + 依赖闭环校验排期）、测试/QA（五态含阻塞适配缺陷流转）、运维/SRE（变更台账 + 内网 URL/IP 脱敏）、技术负责人（owner/scope 过滤查负载、check 统一多人多 AI 产出）、独立开发者（跨会话工作记忆）均适用；诚实边界：轻量文件型任务管理，无甘特图/看板/通知，Jira 级项目管理与产物不进 git 的场景不适用。落在 getting-started「谁适合用」节（角色表 + 边界）与 faq「非开发角色能用吗」条目
- 归档后补记（用户追加决策，已落任务）：**工时统计评估**——分两层：周期统计（created/completed 结构化数据零字段推导，服务项目经理/技术负责人的吞吐与周期场景）可行且有价值，用户拍板**纳入 1.7.0**（新任务「任务周期统计视图」已建档）；精确打卡型工时与零负担定位冲突（AI 无法代填真实人工时长）明确不做，getting-started 边界行补「打卡型工时统计」一词
- 归档后补记（用户追加要求，已落任务）：**文档 ↔ 真实能力双向核对**进 p3 验收矩阵（README重构与三者一致性验收任务 B 节）——正向：文档/skills 写了的能力真实必须支持（1.7.0 预写项以 P2 实现完成为准，发现缺口补实现或删文案）；反向：CLI/skills 真实支持的能力（导入导出、过滤视图、normalize、脱敏规则等）文档必须写到，发现缺口补文档
- 归档后补记（用户追加需求，已完成）：**双全局推荐方案落档**——评估结论：ai-rules.md 已完整表达双全局（✅），但人类读者主路径（README/getting-started/faq）未把「工具 + skills 都全局装 = 个人多项目推荐方案」显式表现出来（❌），skills/README 仅弱覆盖（⚠️）；处理：按使用者身份分流——getting-started 安装导语改「团队项目推荐方式一 / 个人多项目推荐双全局」，faq 新增「个人用，全局装还是每个项目里装？」条目（双全局命令 + 版本漂移诚实代价 + 团队 devDep 对照），README 安装命令注释身份化，skills/README 强化「单人/个人多项目推荐 -g 全局」
- 归档后补记（用户追加要求，已落任务）：**全文档歧义终检**进 p3 验收矩阵（README重构与三者一致性验收任务 B 节）——发版前通读全部文档（docs 八篇 + README + SPEC + skills），逐篇核对能力描述口径一致、解释无歧义（术语首现有解释、类比不与正文冲突）、交叉引用与互链正确、示例与正文命令一致，发现歧义即改

---

## 20260905-唐启云-降级commander至Node20兼容版本

> 负责人：唐启云　状态：已完成　范围：toolkit　完成时间：2026-09-05 01:00

# 降级 commander 至 Node 20 兼容版本

## 背景

commander ^15.0.0 要求 Node >=22.12，与本包 engines.node >=20 冲突，Node 20 环境安装即告警。

## 实施方案

- package.json `commander ^15.0.0` → `^14.0.3`（14 系最新，engines 为 >=20）
- CLI 仅用 command/option/description/version/parse 基础 API，v14 完整支持，代码零改动
- 重新生成 lockfile

## 影响范围

- 仅 package.json 与 lockfile，src/ 零改动

## 验证方式

- pnpm install 无 engine 警告
- 全量门禁：typecheck / lint / test / build
- CLI 冒烟：toolkit --help / tasks --help / changelog --help / 无效参数报错

## 完成说明

- package.json commander ^15.0.0 → ^14.0.3，pnpm install 无 engine 告警，lockfile 已更新
- CLI 代码零改动；typecheck / lint / test（15 文件 75 用例）/ build 全部通过
- CLI 冒烟：--help / tasks --help / changelog --help / 未知命令回退均正常
