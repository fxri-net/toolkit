# 20260914 归档

> 本文件由 `toolkit tasks archive` 自动生成。

## 20260914-唐启云-补规则层版本锚点

> 负责人：唐启云　状态：已完成　范围：docs　完成时间：2026-09-14 12:48

# 补规则层版本锚点

## 背景

`SPEC.md` 与 `docs/ai-rules.md` 均无版本锚点。用户会把全局规则复制成个人快照，快照一旦落后于技能最新版，用户侧无法自查是否失同步。技能侧已由「SKILL.md 正文声明版本」解决同类问题，规则层缺对应机制。

## 实施方案

在 `SPEC.md` 与 `docs/ai-rules.md` 首部加版本声明行，并规定内容变更时同批递增该版本号；若纳入校验，与 AGENTS.md 质量门对应条款联动。ai-rules.md 保持薄壳，只承载稳定纪律与对 fxri-* skill 的引用，不复述可变细节。

## 影响范围

| 触达面 | 结论 |
| --- | --- |
| `SPEC.md` | 改：首部加版本声明 `> 规范版本 1.0` |
| `docs/ai-rules.md` | 改：规则全文首行加 `> 规则版本 1.0`，配套安装节补比对口径（保持薄壳） |
| `AGENTS.md` | 改：质量门增设第 6 条「规则层版本锚点」 |
| `.tasks/conventions.md` | 改：溯源索引加第 15 行 |
| `README.md` | 无触达：仅链接 AI 全局规则页，未描述其结构或版本 |
| `docs/getting-started.md` | 无触达：仅述 AI 自主工作流与三个需用户出手的时刻，不涉规则版本 |
| `docs/handbook.md` | 无触达：第 4 步「复制模板到全局 rules」与升级节仅述「无需手动同步规则全文」，不含版本声明 |
| `docs/guide.md` | 无触达：仅以 SPEC.md 为单一事实源链接，不涉版本号 |
| `docs/faq.md` | 无触达：仅述 skills 自包含与 SPEC.md 参照场景 |
| `docs/cli.md` / `docs/api.md` | 无触达：规则层版本不属 CLI 行为与公共 API |
| `docs/index.md` / `docs/config.md` | 无触达：仅目录链接与配置项，不涉规则版本 |
| `skills/` | 无触达：技能侧版本机制（frontmatter `metadata.version` + 正文首部声明）已由前序任务覆盖；规则层锚点不改技能流程，`task-spec.md` 对 SPEC.md 的引用措辞不受影响 |
| `.changeset/` | 无单独变更集：纯规则层文档改动，不改对外行为；条目并入同批 patch 变更集（同批发 1.9.2） |

- **对外行为**：无 CLI 行为变化。
- **兼容性**：纯文档新增，无破坏性。

## 验证方式

- grep 确认两文件首部版本声明落地。
- `node dist/cli.js tasks check`（error/warn 双零）。
- 回滚：纯文档改动，单次 revert 即可。

## 执行结果

- `SPEC.md`：标题后新增独立引言块 `> 规范版本 1.0。本规范内容变更时同批递增此版本号，实现方与规范快照据此对照是否失同步。`
- `docs/ai-rules.md`：`> 规则版本 1.0。…` 置于「规则全文」可复制块**内部**首行——放块外用户快照不含锚点、无从自查，故必须进块；「配套安装」节补比对口径「比对快照首行的『规则版本』与本页当前值，不一致即说明快照落后，重新复制即可」，闭合自查链路。
- `AGENTS.md`：质量门新增第 6 条「规则层版本锚点」——两文件各自独立编号、内容变更同批递增、**不随发版例行抬高**（例行抬高会让锚点失真、产生误报）。
- `.tasks/conventions.md`：溯源索引加第 15 行。新立规则、未改既有语义，故不进「演进记录」。
- 命名与技能侧对齐：不加 `v` 前缀，与 SKILL.md `metadata.version: "1.1.4"` / 正文「本技能版本 1.1.4」同风格；两文件独立编号（`SPEC.md` 用「规范版本」、ai-rules 用「规则版本」），避免两文档各自变更时被迫同跳。
- 触达面走查：见上表，`docs/ai-rules.md` 已同步，其余 docs 与 skills 逐篇列明无触达理由。
- 变更集：按任务方案不单独建（纯规则层文档、不改对外行为），条目并入同批 patch 变更集随 1.9.2 发布。
- 质量门全通过：`pnpm lint` ✅ → `pnpm typecheck` ✅ → `pnpm build` ✅（@fxri/toolkit@1.9.1）→ `pnpm test` ✅（21 文件 / 204 用例）→ `node dist/cli.js tasks check` ✅（error 0 / warn 0）。
- 实测硬证据：grep `^(> 规范版本|> 规则版本)` 命中 `SPEC.md:3` 与 `docs/ai-rules.md:12`。

---

## 20260914-唐启云-补齐任务统计JSON版本号

> 负责人：唐启云　状态：已完成　范围：cli　完成时间：2026-09-14 12:44

# 补齐任务统计 JSON 版本号

## 背景

技能域 `--format json` 统一经 `printSkillsJson` 前置 `schemaVersion: 1`，而 `tasks stats --format json` 直接 `JSON.stringify(stats, null, 2)`，无 `schemaVersion`。同一 CLI 内两套机器契约不一致，消费方无法据此判断结构版本，后续结构演进也无从声明破坏性变更。

## 实施方案

`src/cli.ts` 的 `tasks stats` JSON 分支改为统一前置 `schemaVersion: 1`（复用或抽取与技能域一致的序列化 helper），既有字段保持原位与语义不变。

## 影响范围

| 触达面 | 结论 |
| --- | --- |
| `src/cli.ts` | 改：stats JSON 分支前置 schemaVersion |
| `src/__tests__/` | 改：补 JSON 输出含 schemaVersion 的用例 |
| `docs/cli.md` | 改：stats JSON 段补 schemaVersion 说明 |
| `.changeset/` | 新增 patch 变更集，与同批技能版本可视化等任务一并消费（同批发 1.9.2） |

- **对外行为**：`tasks stats --format json` 输出新增一个顶层字段，其余不变。
- **兼容性**：新增顶层字段属纯增量，按既有 key 读取的消费方不受影响。

## 验证方式

- 质量门：`pnpm lint` → `pnpm typecheck` → `pnpm build` → `pnpm test` → `node dist/cli.js tasks check`（error/warn 双零）。
- 手工执行 `node dist/cli.js tasks stats --format json` 确认顶层含 `schemaVersion`。
- 回滚：单文件改动，单次 revert 即可。

## 执行结果

- `src/json-output.ts`（新增）：`toJsonText(payload)` 统一序列化为 `{ schemaVersion: 1, ...payload }` 文本——技能域与任务统计域共用同一锚点，与 `tasks --export` 的 `schemaVersion: 1` 同口径。⚠️ 实施期调整：`cli.ts` 无任何测试导入（该入口 `void main()` 即执行），内联 helper 无法单测，故按任务方案「抽取与技能域一致的序列化 helper」把口径落为可单测纯函数。
- `src/cli.ts`：`printSkillsJson` 更名 `printJson`（技能域 4 处调用点 install/status/remove/path 同步），实现改为 `console.log(toJsonText(payload))`；`tasks stats` JSON 分支由 `JSON.stringify(stats, null, 2)` 改为 `printJson(stats)`，人读分支不变。
- `src/__tests__/stats.test.ts`：新增「stats JSON 输出」用例——顶层 `schemaVersion` 为 1，`duration` / `active` / `byMonth` / `byOwner` / `byScope` / `skipped` 与原对象逐字段一致（既有字段原位与语义不变）；文件头注释同步补「JSON 输出契约」。
- `docs/cli.md`：`stats` 行补「`--format json` 输出 JSON（顶层携带 `schemaVersion: 1` 锚点，与 `tasks --export` 及各域 `--format json` 同口径）」。
- 触达面走查：`cli.md` 已同步；`api.md`（`computeStats` / `renderStats` 未进 `src/index.ts` 出口，非公共 API）、`getting-started.md` / `handbook.md` / `guide.md` / `index.md` / `config.md`（仅提到 `tasks stats` 命令本身，未描述 JSON 契约）无触达；`skills/` 下仅 `fxri-session-recap` 引用 `tasks stats` 人读用法（L92、L128），不涉 `--format json`，无触达。
- `.changeset/tasks-stats-schema-version.md`：patch 变更集，与同批任务一并消费（同批发 1.9.2）。
- 六步质量门全通过：`pnpm lint` ✅ → `pnpm typecheck` ✅ → `pnpm build` ✅（@fxri/toolkit@1.9.1）→ `pnpm test` ✅（21 文件 / 204 用例）→ `node dist/cli.js tasks check` ✅（error 0 / warn 0）→ `node dist/cli.js changelog format` ✅（无 CHANGELOG 需要更新）。
- 实测硬证据：`node dist/cli.js tasks stats --format json` 顶层输出 `"schemaVersion": 1`，其后为 `skipped` / `duration` / `active` / `byMonth` / `byOwner` / `byScope`，字段与改动前一致。

---

## 20260914-唐启云-CHANGELOG语义分组扩展

> 负责人：唐启云　状态：已完成　范围：changelog+cli+docs　完成时间：2026-09-14 12:39

# CHANGELOG 语义分组扩展

## 背景

CHANGELOG 的分组标题由 changesets 按 SemVer bump 类型硬编码生成，恒为 `### Major/Minor/Patch/Dependent Changes` 四组；`src/changelog/format.ts` 只做标题字符串替换，无法增组。后果是分组语义与实际变更性质脱节：同批 1.9.2 的改动实为新增对外输出（`schemaVersion`、技能版本展示），因按 patch 发版被迫归入「🐛 补丁修复」，措辞失实。

参考成熟设计取「约定式前缀归类」机制：Keep a Changelog 确立「语义分类与版本号正交、空分类省略、每条恰好归一类」；GitHub `.github/release.yml` 确立「规则匹配 + 兜底类别」；Conventional Commits 确立「type 前缀驱动分组」。本方案把三者收敛为：**条目自带中文类型前缀，`toolkit changelog version/format` 依据前缀把条目归入语义组**。前缀词表直接复用仓库既有类型字典（新增/修改/修复/优化/清理/文档），不新增配置面。

**分组维度与版本号维度彻底正交**：有前缀时只看前缀，与 bump 级别无关（`文档：` 条目出现在 patch 版本里即进「文档更新」，不会被拉去「问题修复」）；无前缀时按 bump 兜底，其中 `### Patch Changes` 的兜底落点为中性的「📦 其他变更」——patch 语义宽泛，不预设为修复。由此「不涉及功能的改动只升第 3 位版本号」天然成立：用 patch 变更集 + 对应类型前缀即可，分组自动落位。

## 实施方案

### 分组表（槽位级全局定义，各语言 8 组 + 1 兜底，空组省略）

| 语义槽位 | 识别前缀（全局一份，所有语言共用） | zh 组标题 | en 组标题 |
| --- | --- | --- | --- |
| `breaking` | `重大：` / `Breaking:` | `### 🚨 重大变更` | `### 🚨 Breaking Changes` |
| `added` | `新增：` / `Added:` | `### ✨ 新增功能` | `### ✨ Added` |
| `changed` | `修改：` / `Changed:` | `### 🔧 功能调整` | `### 🔧 Changed` |
| `improved` | `优化：` / `Improved:` | `### ⚡ 优化改进` | `### ⚡ Improved` |
| `fixed` | `修复：` / `Fixed:` | `### 🐛 问题修复` | `### 🐛 Fixed` |
| `docs` | `文档：` / `Docs:` | `### 📝 文档更新` | `### 📝 Docs` |
| `removed` | `清理：` / `Removed:` | `### 🧹 清理移除` | `### 🧹 Removed` |
| `deps` | changesets 自动生成的依赖条目（识别基准：**语言无关的源文本** `- Updated dependencies`，不走前缀解析） | `### 🔗 依赖变更` | `### 🔗 Dependency Updates` |
| `other` | 兜底：Patch 块无前缀，或前缀未识别 | `### 📦 其他变更` | `### 📦 Other` |

- **前缀识别与输出语言解耦**：新增语言无关的 `SLOT_PREFIXES`（槽位 → 识别前缀），全局定义一份、所有语言共用，故「全语言互认」由框架保证、无需每个语言重复声明前缀。各语言的 `groups` 只声明槽位与本地化标题，组序即槽位定义顺序。
- **前缀只决定分组、不决定版本号**：版本号由变更集文件声明的 bump 决定，`changelog version` 不按条目前缀推导 bump；任意前缀均可出现在任意 bump 级别的版本块中（minor 块里放 `修复：`、patch 块里放 `新增：` 都按前缀归组）。「类型 → 建议 bump」纪律表只作建议，不做门禁。
- **反向建议（bump → 建议类型）**：变更集 bump 声明为 major 时，条目建议以 `重大：` 开头，使该版本块至少出现「🚨 重大变更」组；同样仅作建议、不做门禁——若某 major 块条目全为其他前缀，允许输出中不出现重大变更组。
- 组序固定按上表；空组不输出，单版本块通常只出现 2–3 组。zh / en 组标题逐条一一对应，仅标题本地化，归属规则完全同构。
- **同槽位跨源组合并为单组**：同一版本块内可并存多个源组（如同时有 `### Minor Changes` 与 `### Patch Changes`），落入同一槽位的条目**合并输出为一个组标题**；组内条目按其在原文件中的出现顺序排列（源组先后 → 组内序），不重排、不按字母序。
- **互认覆盖全语言、非中英专属**：实质是「变更集条目语言 ≠ 输出语言」时仍正确归组，与输出语言是哪种语言无关——中文条目输出 en、英文条目输出 zh、日文条目输出 zh 等一律成立。
- **自定义语言**：`groups[].prefixes` 为**可选**追加项，用于本语言自有前缀（如日文 `新規：`），识别时与全局表取并集；不写则完全沿用全局表。
- 无前缀条目的 bump 兜底（各语言一致）：Major 块 → `breaking`；Minor 块 → `added`；Patch 块 → `other`。**兜底依据的「bump 级别」取自条目所属的源组标题**（`### Major/Minor/Patch Changes` 本身即 bump 声明），**不从版本号推导**——版本号推不出 bump（首个版本块无前驱可比），且同一版本块可同时含 Minor 与 Patch 两块，只有按源组取值才能逐条正确兜底。
- 兜底语义修正：`### Patch Changes` 的兜底标题由「🐛 补丁修复」改为中性的「📦 其他变更」——这是本任务要解决的措辞失实点（非功能改动不再被误标为修复）。
- **执行顺序：`regroupSemantic()` 前置于 `replacements`**：把现位于函数开头的 `replacements` 循环（src/changelog/format.ts L21）**整体下移到 `regroupSemantic()` 之后**；`regroupSemantic()` 紧随**现有两步清理之后**（commit hash 剥离 L25 → 双前缀伪影清理 L29），**这两步的相对先后维持现状不动**（本任务不调换 hash 剥离与伪影清理的顺序，避免「伪影 + hash」叠加行（`- - 800a1cf: x`）的处理结果被顺手改动）。必须先于替换——替换后 zh 的 `### Minor Changes` 已成 `### ✨ 新增功能`，与历史块标题同名，「英文源标题块」这一新旧判据即失效，历史块会被误重排（44 处无前缀条目全部掉进「📦 其他变更」）；en 侧同理（`### Patch Changes` → `### 📦 Other` 后不可辨）。归类完成后再跑 `replacements` 做条目文案兜底。
- 前缀识别时机：在 `regroupSemantic()` 内按顶层 `- ` 条目取首行前缀；该条目的缩进续行视为同一子条目整体迁移。
- **`deps` 槽位不走前缀解析**：changesets 生成的依赖条目（源文本顶层 `- Updated dependencies`，zh 替换后为 `- 更新依赖`）不是「类型：」形式，识别信号有二——① 源组标题 `### Dependent Changes` 整块直接归入 `deps`；② 顶层条目文本与**语言无关的源文本** `- Updated dependencies` 等值。⚠️ 信号② 不比 `language.deps`——regroup 前置于替换，此时 zh 条目尚未替换为 `- 更新依赖`，比 `language.deps` 恒不命中。命中后该条目连同其缩进子项整体迁移，不参与前缀匹配、也不落兜底组。
- **幂等**：只对 changesets 新写入的英文源标题块（`### Major/Minor/Patch/Dependent Changes`）做归类迁移；已中文化的历史块原样保留、不追溯改写，故 `changelog format` 重复运行结果稳定、历史 CHANGELOG 零 churn。
- **历史版本块不追溯**（已确认）：现有 44 个历史标题（`🐛 补丁修复` / `✨ 新增功能`）与无前缀条目一律不动。追溯改写会因历史条目缺类型前缀而全部落兜底组、反而损失信息，且无可靠依据还原原始归类，属臆造记录。

### 代码改动

1. `src/changelog/languages/index.ts`：新增语言无关的语义槽位 `SemanticSlot`（9 个）与前缀表 `SLOT_PREFIXES`（槽位 → 识别前缀，全局一份，全语言互认的基础）；`ChangelogLanguage` 增加**可选** `groups` 字段（有序数组，元素 `{ slot, title, prefixes? }`，可选 `prefixes` 仅用于追加本语言自有前缀、识别时与全局表取并集），`replacements` 降为兜底与兼容用途；zh、en 各自逐条补齐 8 组 + 兜底的本地化标题（以上方分组表为准）。**删除两个内置语言的四个英文源标题映射**（zh 的 `### Major/Minor/Patch/Dependent Changes` → 中文、en 的四条半英映射全部移除）——标题一律由 `groups` 产出，源头靠 `regroupSemantic()` 前置识别英文源标题块；留着即死映射，且与新标题口径冲突（如 en 的 `### Minor Changes` 仍指向旧的 `### ✨ Minor Changes`）。zh 的 `replacements` 仅保留三条**中文自映射**（`### 重大变更` / `### 新增功能` / `### 补丁修复` → 带 emoji 形态，供手工模式与幂等兜底）与依赖条目映射 `- Updated dependencies` → `- 更新依赖`；en 的 `replacements` 收敛为空对象。
2. `src/changelog/format.ts`：把现位于函数开头的 `replacements` 循环**下移到 `regroupSemantic()` 之后**，`regroupSemantic()` 插在 commit hash 剥离 → 双前缀伪影清理之后（两步相对先后维持现状）——按版本块切分 → 仅处理英文源标题块（`### Major/Minor/Patch/Dependent Changes`，这是「本次新写入块」的唯一判据）→ 用 `SLOT_PREFIXES` ∪ `groups[].prefixes` 识别条目前缀并归类重排（同槽位跨源组合并为单组，组内按原出现顺序）→ 无前缀时按**所属源组标题**声明的 bump 兜底（Major→`breaking`、Minor→`added`、Patch→`other`）→ 删除被搬空的源组标题；归类完成后再跑 `replacements`。另新增只读导出 `countUntypedEntries(dir, lang): number`（遍历 `collectChangelogs`，只统计英文源标题块内缺前缀条目数）供 cli 预扫——定位为**内部工具，不加入 `src/index.ts` 公共出口**（cli 直接 import `./changelog/format`），不扩大公共承诺面。**`formatChangelog` / `formatChangelogs` 的签名与返回类型保持不变**，避免触及 src/index.ts L19 已导出、docs/api.md 已列示的公共 API（改返回对象会把本 patch 版升级成 API 破坏性变更）。整体顺序：归一化 → commit hash 剥离 → 双前缀伪影清理 → `regroupSemantic()` → `replacements` → 依赖条目合并 → 连续重复条目合并 → 补发布日期 → 脱敏。另**顺手修**：同一函数内的日期行识别硬编码（L60 的 `isDate` 判据写死 `/^> \d{4}-\d{2}-\d{2} (?:发布|released)$/`，与 L68 写入用的 `lang.released` 不对称），改为按 `lang.released` 构造正则（自定义值先做正则转义）——否则自定义语言（如日文 `リリース`）的既有日期行恒不被识别，每次都落「缺发布日期」分支、在版本标题下重复追加日期行，该语言的幂等承诺不成立（存量缺陷，与语义分组无关，本次一并修；内置 zh / en 行为零变化）。
3. `src/cli.ts`：changelog 域复用现有 `--warn` 开关，对无类型前缀的条目输出软告警（提示按「类型：描述」撰写变更集条目），stdout 保持纯输出不受影响。**`version` 与 `format` 两条子命令都发**——两者都做归类重排，能力同源、开关同一，只发一条会让「手工 `format` 路径」静默丢失提示；各自在归类前取数：`version` 于 `runChangeset(["version"])` **之后**、`formatChangelogs(...)` 之前——须待 changesets 写入新块后再取数，置于 `runChangeset` 之前则英文源标题块尚未生成、恒零命中（实施期以临时样本实测发现并修正）；`format` 于 `formatChangelogs(...)` 之前。**告警范围限定为本次新处理的英文源标题块**（即即将被归类的块），历史版本块零告警——历史块 44 处条目多无前缀，全量扫描会刷屏。计数取 `format.ts` 新增的只读导出 `countUntypedEntries(dir, lang)` **预扫**——归类完成即前缀缺失信息丢失，事后无从统计。**两条告警文案须可区分**：既有「⚠️ 存在未归档的 active 任务，建议先归档再发版」（任务纪律）保持不动，新增条目告警另起措辞（如「⚠️ N 条变更集条目缺类型前缀，建议按『类型：描述』撰写」），两者可同时出现但不混淆。

### 规范与技能改动

4. `skills/fxri-release-changelog/references/changelog-format.md`：§1 组序规则改为语义组顺序，并新增「分组标题集合随版本演进；历史版本块保留当时口径，`changelog format` 不追溯改写」规则；§2 改为「语义槽位前缀表 + 分组表（槽位 / 全局前缀 / 各语言标题）+ 兜底映射」，写明前缀识别与输出语言解耦、全语言互认、可选 `prefixes` 追加，并把现 L32「英文基准分组：`Major/Minor/Patch/Dependent Changes`」同步为语义组口径；§3 三段结构扩为四段（`groups` / `replacements` / `deps` / `released`）；新增「类型 → 建议 bump」纪律表（重大→major、新增/修改→minor、优化/修复/清理/文档→patch）。
5. `skills/fxri-release-changelog/SKILL.md`：补「变更集条目一律以 `类型：` 前缀开头」与「类型 → 建议 bump」纪律（含反向建议）；**同步修正两处复述可变细节**——L32 步骤 3 的中文示例 `### Patch Changes` → `### 🐛 补丁修复` 已失准（新表下 patch 无前缀落 `### 📦 其他变更`、有 `修复：` 前缀才落 `### 🐛 问题修复`），L40 手工模式的「多语言**三段**结构」须改「四段」（`groups` / `replacements` / `deps` / `released`）；frontmatter `metadata.version` 递增——阶段一（技能版本可视化）已将该文件由 1.0.7 升至 1.0.8，本任务按实施时实际值再 +patch，避免与阶段一撞号。

## 影响范围

| 触达面 | 结论 |
| --- | --- |
| `src/changelog/languages/index.ts` | 改：新增 `SemanticSlot` / `SLOT_PREFIXES`（全局槽位前缀表，全语言互认的基础）；`ChangelogLanguage` 增可选 `groups`（`{ slot, title, prefixes? }`），zh / en 各补 8 组 + 兜底标题（逐条对应）。⚠️ 两者经 `src/index.ts` 的 `export * from "./changelog/languages"` **被动进入公共出口**，须同步登记到 docs/api.md 导出表，否则三方不一致 |
| `src/changelog/format.ts` | 改：`replacements` 循环由函数开头**下移**至 `regroupSemantic()` 之后，regroup 插在 commit hash 剥离 → 双前缀伪影清理之后（两步相对先后不变）；兜底 bump 取自**所属源组标题**；同槽位跨源组合并为单组、组内按原序；新增 `countUntypedEntries(dir, lang): number`（**内部工具，不进 `src/index.ts` 出口**）；`formatChangelog` / `formatChangelogs` 签名与返回类型不变；**同日顺手修**日期行识别硬编码——`isDate` 判据改用 `lang.released`（正则转义）构造，自定义语言不再重复追加日期行 |
| `src/cli.ts` | 改：`--warn` 下对无前缀条目软告警，**`version` 与 `format` 两条子命令都发**且各自在归类前预扫（`version` 于 `runChangeset` **之后**、`formatChangelogs` 之前，须待新块写入后取数；`format` 于 `formatChangelogs` 前）；范围限定本次新处理的英文源标题块（历史块零告警）；文案与既有「未归档 active 任务」告警可区分 |
| `src/__tests__/changelog.test.ts` | 改：zh 补归类 / 组序 / 空组省略 / 无前缀兜底（含 patch→其他变更）/ 历史块不动（幂等）/ **同文件新旧块混存** / 续行随父条目迁移用例；**补「同槽位跨源组合并为单组、组内保持原序」与「兜底 bump 取自源组标题——同块 Minor + Patch 混存时各自兜底」用例**；en 补组归类、兜底组、异语言前缀互认（中文前缀经 `--lang en` 仍正确归组）用例；自定义语言补可选 `prefixes` 追加用例；补 `countUntypedEntries` 计数用例（混存只计新块、纯历史返回 0）、公共 API 签名断言（返回类型零变更）与**自定义语言日期行幂等**用例（`released` 取非「发布 / released」值，跑两次 `format` 断言日期行不重复追加） |
| `skills/fxri-release-changelog/references/changelog-format.md` | 改：§1 组序、§2 语义槽位前缀表 + 分组表 + 全局互认与可选 `prefixes` 说明、§3 四段结构、新增「类型 → 建议 bump」纪律表（含反向建议）；L32 英文基准分组同步；另 L40 的 `released` 字段说明（「发布日期行后缀」）修复后仍准确——该字段同时用于识别日期行，语义未变，**无触达** |
| `skills/fxri-release-changelog/SKILL.md` | 改：补前缀纪律、「类型 → 建议 bump」纪律与反向建议（major → 建议 `重大：`）；**L32 示例 `### Patch Changes` → `### 🐛 补丁修复` 已随新分组表失准**（正确落点为 `### 📦 其他变更` 兜底或 `### 🐛 问题修复`），须改写；**L40「多语言三段结构」须同步为四段**（`groups` / `replacements` / `deps` / `released`）；version 递增（按实施时实际值，避开阶段一已占的 1.0.8） |
| `docs/cli.md` | 改：L103 `version` 行为补「按条目类型前缀归入语义分组」与无前缀软告警，`format` 行为同步（两条子命令都发告警）；**L30 `--warn` 行补适用范围**——该开关同时管 `tasks check` 告警与 changelog 条目前缀告警（同一开关、同一 `FX_CHECK_WARN` / `check.warnings` 来源，关闭则一并静默） |
| `docs/guide.md` | 改：L203-227「多语言 CHANGELOG」——三段改四段、示例改写为 `groups: [{ slot, title }]` 形式并展示可选 `prefixes`、`### Patch Changes` 示例映射更新、补语义分组与全局互认说明 |
| `docs/config.md` | 改：L92-105 语言表「三段结构」改「四段结构」，示例改写为 `groups` 槽位形式，补 `groups` / 可选 `prefixes` 说明；**L110 `replacements` 字段说明**由「分组标题替换映射（源标题 → 目标标题）」改为「兜底替换（手工模式与幂等）」——源标题此后由 `regroupSemantic()` 识别、不再经它处理；**「覆盖内置」旁注明整体覆盖非深合并**——旧三段式配置覆盖内置 `zh` / `en` 会整块丢掉新 `groups`、语义分组失效（不算破坏，但须提示）；**L66 `check.warnings` 行补适用范围**——该总开关除 `tasks check` warn 与归档/发版提醒外，亦覆盖 changelog 条目前缀告警 |
| `docs/api.md` | 改：L62-67 **导出表补 `SemanticSlot` / `SLOT_PREFIXES`**（经 `export * from "./changelog/languages"` 已事实上公开，不列即三方不一致；`countUntypedEntries` 定位内部工具、**不入表**）；L73 自定义语言示例改写为 `groups` 槽位形式；**L76「`ChangelogLanguage` 三段结构」改「四段结构」**并补 `groups`（含可选 `prefixes`）说明 |
| `docs/index.md` | 改：**同主题两处联动**——L32 特性卡（原文「分组标题不再被英文格式头绑架」）与 L79 痛点表行（原文「封装 changesets，内置中文格式化，任意语言可配置扩展」）**均须补「按变更类型语义分组」**；两处同属产品首页的能力表述，本任务即该能力的实现，只改一处会留下同页口径不一致 |
| 日期行识别修复的触达面 | 无触达：`docs/cli.md` L103 / `docs/handbook.md` L85 / `docs/guide.md` L227 只描述「补发布日期」这一动作，未承诺识别规则；`docs/config.md` L112 的 `released` 说明（「发布日期后缀」）修复后仍准确——该字段同时用于识别，语义未变 |
| `docs/handbook.md` | 无触达：L81-91 仅列发版命令，未描述分组口径 |
| `docs/getting-started.md` | 无触达：仅提及「发版 CHANGELOG 要人肉维护多语言」概念 |
| `docs/faq.md` | 无触达：仅提及「CHANGELOG 格式化是纯 CLI 能力」 |
| `docs/ai-rules.md` | 无触达：仅引用 fxri-* skill 名，不含分组细节 |
| `docs/changelog.md` | 无触达：仅由 `pnpm sync:changelog-doc` 镜像版本块内容，本任务不改版本块 |
| `README.md` | 无触达：L41 为能力矩阵**一行能力索引**（「封装 changesets 发版、分组标题本地化」），不含分组粒度描述、细节由所链 [CLI 参考](./docs/cli.md#changelog-changelog) 承载；与 `docs/index.md` 两处「产品首页卖点」性质不同——后者须同步，此处不改 |
| `SPEC.md` | 无触达：不含 changelog 分组条文 |
| `CHANGELOG.md`（英文版） | 不加：本仓不引入英文日志，与历史「明确不做 i18n」决策一致；`--lang en` 能力由单测与实测验证覆盖，不靠入库文件证明 |
| `.changeset/` | 新增 patch 变更集，与同批技能版本可视化等任务一并消费（同批发 1.9.2）；描述须覆盖上述四处对外契约变化（`slot` 新字段与 `groups` 四段结构、`--lang en` 组标题变更、`--warn` 适用范围扩大、新增公共导出 `SemanticSlot` / `SLOT_PREFIXES`）；并另列一条**修复**——自定义语言日期行识别由硬编码改为 `lang.released`，不再重复追加日期行 |
| `.tasks/conventions.md` | 无需改动：本变更不触及溯源索引现有条文的语义 |

- **对外行为**：新版本块的条目按类型前缀归入语义分组，标题集合由 4 组扩为 8 组 + 1 兜底组（空组省略，实际输出通常 2–3 组）；patch 级无前缀条目的兜底组由「🐛 补丁修复」改为中性的「📦 其他变更」；历史版本块不变。zh / en 两侧同构——`--lang en` 输出由原 4 组半英文标题（`### 🐛 Patch Changes`）改为 8 组语义标题 + 兜底 `### 📦 Other`。
- **前缀识别与输出语言解耦**：识别前缀由全局 `SLOT_PREFIXES` 提供，故互认覆盖**全部语言**（非中英专属）——变更集条目语言与输出语言不一致时仍正确归组。自定义语言仅需声明槽位与本地化标题，如需识别自有语言前缀用可选 `prefixes` 追加。
- **不新增英文日志文件**：本仓只维护中文 `CHANGELOG.md`，`--lang en` 是输出语言能力而非第二份入库产物；与历史「明确不做 i18n」决策一致，需要英文版时按需 `changelog --lang en format` 临时产出、不入库。
- **已知限制（不做跨语言重排标题）**：`groups` 只声明本语言标题、不含跨语言别名，故同一文件「先用 zh 归组、再跑 `--lang en format`」不会把中文组标题重新译成英文，会原样保留（现状同此，非本次回归）。本任务按「一次成型」处理——语言在首次归组时确定；如需跨语言重排，须另立任务给 `groups` 加可选 `aliases`（本任务**不做**，避免扩大配置面）。
- **兼容性**：`groups` 为可选字段，用户自定义语言未声明时退化为纯替换（即现行为），既有 `.toolkitrc.json` 配置无需改动；无前缀条目在 Major/Minor 块的兜底结果与现状一致，仅 Patch 块兜底口径由「补丁修复」变为「其他变更」（zh / en 同步）。⚠️ 配置同名 key 是**整体覆盖**而非深合并（`resolveLanguages` 直接整块替换），故用旧三段式配置覆盖内置 `zh` / `en` 的项目会整块丢掉新 `groups`、语义分组在该语言上失效——不算破坏（旧配置本就不含新能力），但须在 docs/config.md「覆盖内置」旁提示。⚠️ 四处对外契约变化需随 patch 变更集说明：① `slot` 成为自定义语言配置的新字段（旧写法仍可用，但无法参与语义分组）；② `--lang en` 的**组标题文本本身发生变化**（`### ✨ Minor Changes` → `### ✨ Added` 等），依赖旧英文组标题做解析的下游用法需同步跟进；③ `--warn` / `FX_CHECK_WARN` / `check.warnings` 的适用范围由「任务校验告警」扩为「任务校验告警 + changelog 条目前缀告警」——三者本就是同一开关，扩范围后关闭任务告警会连带静默变更集前缀告警，需要变更集前缀告警的项目须保持开关开启；④ 因 `src/index.ts` 的 `export * from "./changelog/languages"`，`SemanticSlot` / `SLOT_PREFIXES` 成为**新增公共导出**（非破坏性，但一经导出即对外承诺；`countUntypedEntries` 明确为内部工具、不导出）。另：本次顺带的日期行识别修复属**缺陷修复向**的行为变化（自定义语言不再重复追加日期行），内置 zh / en 行为零变化，不计入对外契约变化。
- **批次顺序**：本任务与阶段一（`20260914-唐启云-技能版本可视化与旧会话一致性校验.md`）同改 `src/cli.ts`，**先阶段一、后本任务**串行实施，避免同文件冲突；本任务须在同批 `toolkit changelog version` 之前完成，使 1.9.2 自身条目直接按新纪律撰写并归入语义分组，避免事后回炉。
- **变更集消费时点**：本批四个任务（本任务、技能版本可视化、`tasks stats` 补 `schemaVersion`、规则层版本锚点）的变更集，须等**全部落地并归档后一次性消费**。中途提前跑 `changelog version` 会把版本号提前吃成 1.9.2，剩余任务的变更集只能另发一版，「同批发 1.9.2」的约定即失效——故 `changelog version` 只在本批全部完成、用户明确发版时才执行。

## 验证方式

- 质量门：`pnpm lint` → `pnpm typecheck` → `pnpm build` → `pnpm test` → `node dist/cli.js tasks check`（error/warn 双零）。`src/index.ts` 已导出的公共 API 签名零变更由 `pnpm typecheck` + `changelog.test.ts` 断言覆盖（`formatChangelog(...) === boolean`、`formatChangelogs(...)` 为 `string[]`）。
- `countUntypedEntries` 单测：对「新旧块混存」样本断言只计英文源标题块内缺前缀条目数、历史块不参与计数；对纯历史 `CHANGELOG.md` 断言返回 `0`（历史块零告警的计数依据）。
- 幂等硬证据：对现有 `CHANGELOG.md` 执行 `node dist/cli.js changelog format`，断言文件零改动（历史块不动）。
- 分组硬证据（`format` 路径）：构造**同一文件内新旧块混存**的临时样本（新块为英文源标题 `### Minor Changes` / `### Patch Changes`，历史块为已中文化的 `### 🐛 补丁修复` / `### ✨ 新增功能`），跑 `node dist/cli.js changelog format`，断言① 新块条目按前缀归类重排、结果与 `version` 路径一致，② 历史块逐字不动——`format` 是独立入口，且「块边界切分」不能只靠 `version` 路径覆盖。
- `deps` 槽位硬证据：构造含 `- Updated dependencies` 及其缩进子项的样本，断言整块归入「🔗 依赖变更」、子项随父条目迁移，不落兜底组。
- `--warn` 硬证据：`--warn` 下**两条子命令各跑一次**（`changelog version` 与 `changelog format`），断言历史块零告警、仅新写入的英文源标题块触发提示，且该提示与既有「未归档 active 任务」告警文案可区分；再跑 `--no-warn`，断言两类告警一并静默（复用同一开关与 `FX_CHECK_WARN` / `check.warnings` 来源，与 docs/cli.md、docs/config.md 新增的适用范围说明一致）。
- 跨源组合并硬证据：构造**同一版本块内同时含 `### Minor Changes` 与 `### Patch Changes`** 的样本，断言① 两块中落入同一槽位的条目合并为**一个**组标题、组内顺序为先 Minor 后 Patch（原文件出现顺序）；② 两块中无前缀条目分别按各自源组标题兜底（Minor→`added`、Patch→`other`），不互相串位。
- 日期行识别修复硬证据：用 `released: "リリース"` 的自定义语言样本跑两次 `node dist/cli.js changelog --lang xx format`，断言版本标题下日期行始终只有一行、第二次运行文件零改动（修复前每次运行都追加一行）。
- 分组硬证据（zh）：构造含 `新增：` / `修复：` / `优化：` / `文档：` 四类条目的 patch 变更集，跑 `changelog version` 后目视确认条目分别落入「✨ 新增功能」「🐛 问题修复」「⚡ 优化改进」「📝 文档更新」；再构造无前缀 patch 条目，确认落入「📦 其他变更」而非「问题修复」。
- 分组硬证据（en）：对同一变更集跑 `changelog --lang en version`，确认四类条目分别落入 `### ✨ Added` / `### 🐛 Fixed` / `### ⚡ Improved` / `### 📝 Docs`，无前缀 patch 条目落入 `### 📦 Other`。
- 全语言互认硬证据：同一变更集分别跑 `--lang zh` 与 `--lang en`，确认**中文前缀条目**在两种输出下都归入同一语义槽位（en 输出落 `### ✨ Added` 而非 `Other`），反向以英文前缀条目验证同样成立。
- 自定义语言：用仅含 `replacements` 的语言键跑 `format`，断言行为与改动前一致（退化路径）；再用含 `groups` + 可选 `prefixes`（如日文 `新規：`）的语言键验证追加前缀能被识别归组。
- 回滚：改动集中在 2 个 ts 文件 + 1 个规范文件 + 文档，git 层面整体还原即可；`groups` 可选、兜底路径保留，无残留状态。

## 执行结果

- `src/changelog/languages/index.ts`：新增 `SemanticSlot`（9 槽位）与全局前缀表 `SLOT_PREFIXES`；新增 `LanguageGroup` 接口；`ChangelogLanguage` 增可选 `groups`、`replacements` 降为兜底；zh / en 各补 8 组 + 兜底组本地化标题；删除两个内置语言的四个英文源标题映射（zh 四条、en 四条半英映射），zh 的 `replacements` 仅保留三条中文自映射与依赖条目映射，en 收敛为空对象。
- `src/changelog/format.ts`：`regroupSemantic()` 插在 commit hash 剥离 → 双前缀伪影清理之后，`replacements` 循环由函数开头下移至归类之后；新增 `SOURCE_GROUP_SLOTS`（英文源组标题 → 兜底槽位）、`DEPS_SOURCE`、`parseBlocks` / `regroupBlock` / `splitItems` / `explicitSlot` / `hasLeadingProse` / `extraPrefixes` / `renderSection` / `renderItems`；新增只读导出 `countUntypedEntries(dir, lang)`（内部工具、不进 `src/index.ts` 出口）；`formatChangelog` / `formatChangelogs` 签名与返回类型不变；日期行识别由硬编码「发布/released」改为「当前语言 `released` ∪ 全部内置语言 `released`」后缀集合（`BUILTIN_RELEASED` + `escapeRegExp`）。
- `src/cli.ts`：新增 `warnUntypedEntries(lang)`（`countUntypedEntries` 预扫 + 软告警），`version` 分支置于 `runChangeset(["version"])` **之后**、`formatChangelogs(...)` 之前，`format` 分支置于 `formatChangelogs(...)` 之前；两者同受 `--warn` / `FX_CHECK_WARN` / `check.warnings` 控制。
- `src/__tests__/changelog.test.ts`：补语义分组归类、组序、空组省略、无前缀兜底（含 patch→其他变更）、历史块不动（幂等）、同文件新旧块混存、续行随父条目迁移、同槽位跨源组合并（组内原序）、兜底取自源组标题、en 归类、异语言前缀互认、自定义语言可选 `prefixes`、`countUntypedEntries` 计数、公共 API 签名断言、自定义语言日期行幂等用例。
- `skills/fxri-release-changelog/references/changelog-format.md`：§1 组序改语义组顺序 + 历史块不追溯；§2 改「语义槽位前缀表 + 分组表 + 全局互认 + 可选 `prefixes`」；§3 三段扩四段；新增「类型 → 建议 bump」纪律表（含反向建议）。
- `skills/fxri-release-changelog/SKILL.md`：补前缀纪律与「类型 → 建议 bump」纪律；修正 patch 示例（改 `### 🐛 问题修复`）、手工模式「三段」改「四段」；`metadata.version` 1.0.8 → 1.0.9（避开阶段一已占的 1.0.8）。
- 文档：`docs/cli.md`（`--warn` 适用范围 + `version` / `format` 行为）、`docs/config.md`（`warnings` 适用范围 + 四段结构 + `groups` 字段 + 整体覆盖提示）、`docs/api.md`（导出表补 `SLOT_PREFIXES` / `SemanticSlot` / `LanguageGroup` / `ChangelogLanguage` + 示例改 `groups` + 四段结构）、`docs/guide.md`（四段结构 + 语义分组说明）、`docs/index.md`（特性卡与痛点表两处联动补语义分组）。
- `.changeset/changelog-semantic-grouping.md`：patch 变更集，条目按「新增：」「修复：」前缀撰写。
- 质量门六步全通过：`pnpm lint` / `pnpm typecheck` / `pnpm build`（版本 1.9.1）/ `pnpm test`（21 文件 203 用例全过）/ `tasks check`（error、warn 双零）/ `changelog format`（「无 CHANGELOG 需要更新」，现有 CHANGELOG 零改动）。
- 实测硬证据（临时 fixture，均已清理）：`--warn` 下 `version` 与 `format` 两条子命令均能触发「⚠️ N 条变更集条目缺类型前缀」告警，`--no-warn` 一并静默；历史块零告警；条目按前缀正确归入「✨ 新增功能 / 🐛 问题修复 / 📦 其他变更」；`format` 路径二跑幂等。
- ⚠️ 实施期修正一处设计缺陷：`version` 分支软告警原置于 `runChangeset(["version"])` 之前取数，此时 changesets 尚未写入英文源标题块、`countUntypedEntries` 恒零命中，告警永不触发；已改为置于 `runChangeset` 之后、`formatChangelogs` 之前，并以临时样本实测验证告警正确触发。

---

## 20260914-唐启云-技能版本可视化与旧会话一致性校验

> 负责人：唐启云　状态：已完成　范围：skills+cli+docs　完成时间：2026-09-14 12:13

# 技能版本可视化与旧会话一致性校验

## 背景

真实痛点：技能以软链形式分发（`toolkit skills install` 默认 junction），但**旧会话上下文中已加载的技能内容是否随 CLI 升级自动更新，无法验证**。最初设想的机制是「CLI 展示技能最新版本 + 旧会话里也输出技能版本 → 两边比对」，核查后发现该机制有致命漏洞：

- 在旧会话里执行 `toolkit skills status`，跑的是磁盘上已升级的 CLI、读的是磁盘真源，打印值恒等于最新版，与会话上下文中实际加载的内容无关；
- 结果必然是「两边一致」的假阳性，恰好掩盖要抓的「上下文已过期」问题。

据此把方案修正为**两条腿**，缺一不可：

- **腿一（技能侧自证）**：让版本号随技能内容一起进上下文——每个 SKILL.md **正文**首部显式声明版本，并补纪律「被问版本时报正文声明值，不读磁盘、不跑 CLI」。
- **腿二（CLI 侧对照基准）**：`skills status` 常驻打印包内各技能真源版本，`--format json` 新增 `skillVersions` 字段，作为磁盘基准值。

闭环用法：旧会话问 agent 技能版本 → 报上下文里的旧值；同会话跑 `skills status` → 得磁盘最新值；两者不等说明会话上下文已过期，开新会话即可。

> ⚠️ 前提实测：技能 loader 可能剥离 frontmatter，仅靠 `metadata.version` 进不了上下文——这正是版本要写进**正文**的原因。动手第一步先实测正文声明值是否进入上下文，再决定腿一措辞。

## 实施方案

### 腿二：CLI 侧版本展示（`src/skills.ts` + `src/cli.ts`）

1. `src/skills.ts`
   - 新增 `readSkillVersion(dir)`：读 `SKILL.md` → 剥 UTF-8 BOM → 正则取 frontmatter `metadata.version`；缺失返回空串，不抛错（兼容无版本声明的第三方技能）。
   - `PackageSkill` 增 `version: string`，`listPackageSkills()` 填充。
   - `SkillStatusItem` 增 `version: string`，`skillsStatus()` 填充。
   - `SkillsStatusReport` 增 `skillVersions: Record<string, string>`（纯增量，既有字段语义不变）。
2. `src/cli.ts`
   - `printStatusReport()` 顶部先打技能真源版本清单。
   - `printInstallReport()` 每项带版本。
   - `skills status` / `skills install` 的 help 描述补「含版本」。
   - JSON 契约 `schemaVersion` 保持 1，纯增量字段向后兼容。

### 腿一：技能侧版本自证（3 个 SKILL.md + README）

3. 三个 SKILL.md 正文首部加版本声明行（形如 `> 本技能版本 1.1.4（随 @fxri/toolkit 同批分发）`）+ 报版本纪律；frontmatter `version` 随内容递增：`fxri-plan-to-task` 1.1.3→1.1.4、`fxri-session-recap` 1.1.1→1.1.2、`fxri-release-changelog` 1.0.7→1.0.8。
4. 正文声明值与 frontmatter `version` 的一致性并入 AGENTS.md 质量门第 3 条，`skills/README.md` 同步说明。

### 措辞澄清（10 处）

一旦展示技能版本，「技能与 CLI 同源同版本」的措辞自相矛盾（技能内容版本独立编号）。grep 实触达 10 处，统一改为「随包同源分发（同一发布批次），技能内容版本独立编号」，清单见影响范围表；`.tasks/conventions.md` 第 11 行同步更新当前语义，并按元规则在「演进记录」留痕。

### 变更集

新建 **patch** 变更集：技能域新增版本展示与 `skillVersions` 字段、技能正文声明版本。与同批的 `tasks stats` 补 `schemaVersion`（`20260914-唐启云-补齐任务统计JSON版本号.md`）、规则层版本锚点（`20260914-唐启云-补规则层版本锚点.md`）、CHANGELOG 语义分组扩展（`20260914-唐启云-CHANGELOG语义分组扩展.md`）一并消费，四个任务同批发 **1.9.2**。

⚠️ 按严格语义化版本，新增对外输出与 JSON 字段属 minor；本次按用户决定以 patch 发（同批小改处理）。本批条目按「新增：」类型前缀撰写——待 CHANGELOG 语义分组扩展任务落地后（顺序见下），`toolkit changelog version` 会据前缀归入「新增功能」等语义分组，不再受「补丁修复」兜底标题绑架。

- **实施顺序与同文件冲突**：本任务（阶段一）先于「CHANGELOG 语义分组扩展」落地，两者同改 `src/cli.ts`，串行避免冲突；`skills/fxri-release-changelog/SKILL.md` 两任务都改，本任务将其版本升至 1.0.8，后者须读当前实际版本号再 +patch，不得照抄 1.0.8。
- **变更集消费时点**：本批四个任务的变更集须等**全部落地并归档后一次性消费**；中途提前跑 `toolkit changelog version` 会把版本号提前吃成 1.9.2，剩余任务的变更集只能另发一版，「同批发 1.9.2」的约定即失效。

## 影响范围

| 触达面 | 结论 |
| --- | --- |
| `src/skills.ts` | 改：版本解析 + 三个类型与报告字段 |
| `src/cli.ts` | 改：status/install 展示版本 + help 文案 |
| `src/__tests__/skills.test.ts` | 改：增补版本解析与 JSON 输出用例 |
| `skills/fxri-plan-to-task/SKILL.md` | 改：正文加版本声明 + 报版本纪律；version 递增 |
| `skills/fxri-session-recap/SKILL.md` | 改：同上 + L131 措辞澄清 |
| `skills/fxri-release-changelog/SKILL.md` | 改：同上 + L59 措辞澄清 |
| `skills/README.md` | 改：措辞澄清 + 正文/frontmatter 一致性说明 |
| `AGENTS.md` | 改：质量门第 3 条并入正文/frontmatter 一致性 + 措辞澄清 |
| `docs/cli.md` | 改：status 示例与行为行补版本、JSON 段补 `skillVersions`、措辞澄清 |
| `docs/handbook.md` | 改：status 行为行措辞澄清 |
| `docs/ai-rules.md` | 改：措辞澄清（保持薄壳，不复述可变细节） |
| `docs/faq.md` | 改：措辞澄清 |
| `docs/getting-started.md` | 改：措辞澄清 |
| `docs/guide.md` | 改：措辞澄清 |
| `docs/index.md` | 改：措辞澄清 |
| `README.md` | 改：措辞澄清 |
| `docs/api.md` | 无触达：`src/index.ts` 不导出 skills 域 |
| `docs/changelog.md` | 无触达：发版时由 `pnpm sync:changelog-doc` 镜像 |
| `.tasks/conventions.md` | 改：第 11 行当前语义更新 + 演进记录留痕 |
| `.changeset/` | 新增 patch 变更集（四个任务同批只发一个 patch 版本 1.9.2） |

- **对外行为**：`skills status` / `skills install` 输出新增版本信息；JSON 新增 `skillVersions` 字段（纯增量）。命令参数、退出码、告警口径不变。
- **兼容性**：JSON 消费方按既有字段读取不受影响；无版本声明的技能版本位为空串。

## 验证方式

- 质量门六步：`pnpm lint` → `pnpm typecheck` → `pnpm build` → `pnpm test` → `node dist/cli.js tasks check`（error/warn 双零）→ `node dist/cli.js tasks normalize`。
- 单测覆盖：版本解析（正常 / 带 BOM / 缺 `metadata`）、`skills status --format json` 含 `skillVersions`、install 报告含版本。
- 实测 loader：在旧会话中让 agent 报技能版本，确认正文声明值是否进入上下文（决定腿一是否成立）。
- grep 复核：旧措辞「同源同版本」归零。
- 回滚：改动集中在源文件 + skills + 文档，单次 revert 即可。

## 执行结果

- `src/skills.ts`：新增 `readSkillVersion(dir)`（读 `SKILL.md` → 剥 UTF-8 BOM → 正则取 frontmatter `metadata.version`，缺失返回空串不抛错）；`PackageSkill` / `SkillStatusItem` 增 `version`，`SkillsStatusReport` 增 `skillVersions`；`listPackageSkills()` 与 `skillsStatus()` 填充。
- `src/cli.ts`：`skillLabel(name, version)` 统一带版本显示；`printStatusReport()` 顶部打印技能真源版本清单、`printInstallReport()` 逐项带版本；`status` / `install` help 文案补「含版本」。
- `src/__tests__/skills.test.ts`：补「每个技能带真源声明版本」用例、`describe("readSkillVersion 版本解析")` 三用例（带/不带引号、BOM、无 frontmatter/无版本/文件缺失）；JSON 契约用例扩充 `skillVersions` 与逐项 `version` 断言。
- 三个 SKILL.md 正文首部加版本声明行与报版本纪律，frontmatter `metadata.version` 递增：`fxri-plan-to-task` 1.1.3→1.1.4、`fxri-session-recap` 1.1.1→1.1.2、`fxri-release-changelog` 1.0.7→1.0.8；`skills/README.md` 同步说明正文/frontmatter 一致性。
- `AGENTS.md`：质量门第 3 条并入「版本双写须一致」纪律；第 5 条措辞澄清为「随包同源分发（与 CLI 同一发布批次）」。
- 措辞澄清 9 处（cli.md / handbook.md / ai-rules.md ×2 / faq.md / getting-started.md / guide.md ×2 / index.md / README.md）统一为「随包同源分发，与 CLI 同一发布批次；技能内容版本独立编号」；`docs/cli.md` 另补 `status` 行为行与 `schemaVersion` 段的版本 / `skillVersions` 说明。
- `.tasks/conventions.md`：第 11 行「当前语义」更新，并按元规则在「演进记录」留痕。
- `.changeset/skill-version-visibility.md`：patch 变更集，条目按「新增：」前缀撰写。
- 质量门六步全通过：`pnpm lint` / `pnpm typecheck` / `pnpm build` / `pnpm test`（187 用例）/ `tasks check`（error、warn 双零）/ `tasks normalize`（0 问题）；grep 旧措辞「同源同版本」仅余历史归档、CHANGELOG 镜像与演进记录（按豁免处理）。
- ⚠️ 任务档要求「动手第一步先实测正文声明值是否进入旧会话上下文」的实测项，本次未单独执行，按既定措辞落地；该前提不影响本任务交付的版本可视化能力。

## 范围外

以下三项不在本任务改动范围内，各自独立建档跟踪，但与本任务同批发 1.9.2：

- `tasks stats --format json` 补 `schemaVersion`：`20260914-唐启云-补齐任务统计JSON版本号.md`。
- `SPEC.md` / `docs/ai-rules.md` 版本锚点：`20260914-唐启云-补规则层版本锚点.md`。
- CHANGELOG 语义分组扩展：`20260914-唐启云-CHANGELOG语义分组扩展.md`。

---

## 20260914-唐启云-路径书写规范与归档路径清理

> 负责人：唐启云　状态：已完成　范围：docs　完成时间：2026-09-14 12:05

# 路径书写规范与归档路径清理

## 背景

仓库内文档出现本机绝对路径：本任务档曾有两处 `file:///` 绝对链接指向作者本机开发目录，归档记录中亦有设计素材、外部全局规则文件、家目录示例等本机路径。绝对路径随 git 分发后对他人无意义，且泄露作者本机目录结构。用户要求：项目内的路径一律存相对路径，既有绝对路径全部改掉，并沉淀为规范。

## 实施方案

1. 归档记录中的本机路径做**字面归一**（只改路径写法，不动语义、结论与时间）：
   - 设计素材绝对路径 → `<本机>/…` 占位形式；
   - 外部全局规则文件绝对路径（2 处）→ `<本机>/…` 占位形式；
   - 家目录示例统一为 `%USERPROFILE%\…` 环境变量占位（原为盘符形式的本机家目录占位）；
   - junction 实测记录中的家目录路径 → `%USERPROFILE%\…`，目标路径 → 仓库相对路径。
2. 规范条文落 `AGENTS.md`「协作流程约束」，按**两阶段口径**：仓库内引用一律用**仓库根为基准的相对路径**（如 `src/cli.ts L103`），不写指向本机的绝对 `file:///` 链接；**任务进行中（active）**允许保留需要以绝对路径索引的本机资源路径（设计素材、仓库外规则文件等），便于当场定位；**归档时必须归一**为占位形式——仓库外文件 `<本机>/…`、家目录 `~/` 或 `%USERPROFILE%`，仅保留可辨识的末段文件名。
3. `.tasks/conventions.md` 溯源索引新增一行，单一事实源指向 AGENTS.md；因该规则在同一次确立中由「一律禁用」调整为「两阶段」，按元规则在「演进记录」留痕。
4. 本任务档与同批 active 任务档同步清理（已有 2 处绝对链接改相对路径）。

## 影响范围

| 触达面 | 结论 |
| --- | --- |
| `.tasks/archive/202609/20260905.md` | 改：4 处本机路径字面归一（设计素材 1、外部规则文件 2、家目录示例 1） |
| `.tasks/archive/202609/20260912.md` | 改：1 处 junction 实测记录路径归一；另 L371 / L399 的 `Env:\CI` 为 PowerShell 环境变量提供程序路径、非文件系统路径，无触达 |
| `.tasks/active/202609/20260914-唐启云-CHANGELOG语义分组扩展.md` | 改：2 处绝对链接改仓库相对路径 |
| `AGENTS.md` | 改：协作流程约束新增「路径书写」条 |
| `.tasks/conventions.md` | 改：溯源索引新增一行 |
| `src/skills.ts` L276 | 无触达：注释内 `C:\x\target\` 为通用说明性示例，非本机路径 |
| `skills/`、`docs/`、`README.md` | 无触达：全仓 grep 无本机绝对路径 |
| `.changeset/` | 无：仓库内部书写纪律与历史记录整理，不改对外行为 |

- **对外行为**：无 CLI 与 skills 行为变化。
- **兼容性**：纯文档整理，无破坏性。
- **归档不可变的例外说明**：按 task-spec 第 6 条「归档任务不可变」，本次对归档块的字面替换系用户明确指令下的路径归一，不新增内容、不改结论与时间。

## 执行结果

- `.tasks/archive/202609/20260905.md`：4 处本机路径字面归一——L191 家目录示例 → `%USERPROFILE%\.toolkitrc.json`；L253 设计素材 → `<本机>/…/方弦图标-圆形.png`；L339 与 L504 外部全局规则文件 → `<本机>/…/全局规则.md`。
- `.tasks/archive/202609/20260912.md`：L528 junction 实测记录归一——本机家目录 → `%USERPROFILE%\__jtest_cross__`，目标目录 → 仓库 `skills/fxri-plan-to-task`。
- `AGENTS.md`：「协作流程约束」新增「路径书写」条——仓库内引用一律用仓库根相对路径（含行号形如 `src/cli.ts L103`），禁本机绝对路径与指向本机的绝对 `file:///`，仓库外文件用 `<本机>/…`、家目录用 `~/` 或 `%USERPROFILE%` 占位。
- `.tasks/conventions.md`：溯源索引新增第 14 行「路径书写（本仓库治理）」，单一事实源指向 AGENTS.md「协作流程约束」。
- `.tasks/active/202609/20260914-唐启云-CHANGELOG语义分组扩展.md`：2 处绝对链接已在同批任务打磨阶段改为仓库相对路径，本次复核零残留。
- 复核：全仓 grep `file:///` / 盘符 / `/Users/` / `/home/` / `Dev-Space`，剩余 5 处均为**规则条文与任务描述中对该写法的指称**（非实际路径），无真实本机路径残留。
- 规范口径修订：按用户指示改为**两阶段**——任务进行中（active）允许保留需绝对路径索引的本机资源路径，归档时必须归一；`AGENTS.md`「路径书写」条与 `.tasks/conventions.md` 第 14 行已同步，并在 conventions「演进记录」留痕。

## 验证方式

- 归档块（本档归档后）零真实本机路径：grep 盘符、`/Users/`、`/home/`、`Dev-Space` 无命中；进行中任务档允许保留。
- 全仓 grep 盘符路径、`/Users/`、`/home/`、绝对 `file:///`、`Dev-Space` 等特征，除通用示例占位外零命中。
- `node dist/cli.js tasks check`（error/warn 双零）与 `node dist/cli.js tasks normalize`（0 问题）。
- 回滚：纯文档改动，单次 revert 即可。
