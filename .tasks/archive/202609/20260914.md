# 20260914 归档

> 本文件由 `toolkit tasks archive` 自动生成。

## 20260914-唐启云-技能版本可视化与旧会话一致性校验

> 负责人：唐启云　状态：已完成　范围：skills+cli+docs　完成时间：2026-09-14 12:13

# 技能版本可视化与旧会话一致性校验

## 背景

真实痛点：技能以软链形式分发（`toolkit skills install` 默认 junction），但**旧会话上下文中已加载的技能内容是否随 CLI 升级自动更新，无法验证**。最初设想的机制是「CLI 展示技能最新版本 + 旧会话里也输出技能版本 → 两边比对」，核查后发现该机制有致命漏洞：

- 在旧会话里执行 `toolkit skills status`，跑的是磁盘上已升级的 CLI、读的是磁盘真源，打印值恒等于最新版，与会话上下文中实际加载的内容无关；
- 结果必然是「两边一致」的假阳性，恰好掩盖要抓的「上下文已过期」问题。

据此把方案修正为**两条腿**，缺一不可：

- **腿一（技能侧自证）**：让版本号随技能内容一起进上下文——每个 SKILL.md **正文**首部显式声明版本，并补纪律「被问版本时报正文声明值，不读磁盘、不跑 CLI」。
- **腿二（CLI 侧对照基准）**：`skills status` 常驻打印包内各技能真源版本，`--format json` 新增 `skillVersions` 字段，作为磁盘基准值。

闭环用法：旧会话问 agent 技能版本 → 报上下文里的旧值；同会话跑 `skills status` → 得磁盘最新值；两者不等说明会话上下文已过期，开新会话即可。

> ⚠️ 前提实测：技能 loader 可能剥离 frontmatter，仅靠 `metadata.version` 进不了上下文——这正是版本要写进**正文**的原因。动手第一步先实测正文声明值是否进入上下文，再决定腿一措辞。

## 实施方案

### 腿二：CLI 侧版本展示（`src/skills.ts` + `src/cli.ts`）

1. `src/skills.ts`
   - 新增 `readSkillVersion(dir)`：读 `SKILL.md` → 剥 UTF-8 BOM → 正则取 frontmatter `metadata.version`；缺失返回空串，不抛错（兼容无版本声明的第三方技能）。
   - `PackageSkill` 增 `version: string`，`listPackageSkills()` 填充。
   - `SkillStatusItem` 增 `version: string`，`skillsStatus()` 填充。
   - `SkillsStatusReport` 增 `skillVersions: Record<string, string>`（纯增量，既有字段语义不变）。
2. `src/cli.ts`
   - `printStatusReport()` 顶部先打技能真源版本清单。
   - `printInstallReport()` 每项带版本。
   - `skills status` / `skills install` 的 help 描述补「含版本」。
   - JSON 契约 `schemaVersion` 保持 1，纯增量字段向后兼容。

### 腿一：技能侧版本自证（3 个 SKILL.md + README）

3. 三个 SKILL.md 正文首部加版本声明行（形如 `> 本技能版本 1.1.4（随 @fxri/toolkit 同批分发）`）+ 报版本纪律；frontmatter `version` 随内容递增：`fxri-plan-to-task` 1.1.3→1.1.4、`fxri-session-recap` 1.1.1→1.1.2、`fxri-release-changelog` 1.0.7→1.0.8。
4. 正文声明值与 frontmatter `version` 的一致性并入 AGENTS.md 质量门第 3 条，`skills/README.md` 同步说明。

### 措辞澄清（10 处）

一旦展示技能版本，「技能与 CLI 同源同版本」的措辞自相矛盾（技能内容版本独立编号）。grep 实触达 10 处，统一改为「随包同源分发（同一发布批次），技能内容版本独立编号」，清单见影响范围表；`.tasks/conventions.md` 第 11 行同步更新当前语义，并按元规则在「演进记录」留痕。

### 变更集

新建 **patch** 变更集：技能域新增版本展示与 `skillVersions` 字段、技能正文声明版本。与同批的 `tasks stats` 补 `schemaVersion`（`20260914-唐启云-补齐任务统计JSON版本号.md`）、规则层版本锚点（`20260914-唐启云-补规则层版本锚点.md`）、CHANGELOG 语义分组扩展（`20260914-唐启云-CHANGELOG语义分组扩展.md`）一并消费，四个任务同批发 **1.9.2**。

⚠️ 按严格语义化版本，新增对外输出与 JSON 字段属 minor；本次按用户决定以 patch 发（同批小改处理）。本批条目按「新增：」类型前缀撰写——待 CHANGELOG 语义分组扩展任务落地后（顺序见下），`toolkit changelog version` 会据前缀归入「新增功能」等语义分组，不再受「补丁修复」兜底标题绑架。

- **实施顺序与同文件冲突**：本任务（阶段一）先于「CHANGELOG 语义分组扩展」落地，两者同改 `src/cli.ts`，串行避免冲突；`skills/fxri-release-changelog/SKILL.md` 两任务都改，本任务将其版本升至 1.0.8，后者须读当前实际版本号再 +patch，不得照抄 1.0.8。
- **变更集消费时点**：本批四个任务的变更集须等**全部落地并归档后一次性消费**；中途提前跑 `toolkit changelog version` 会把版本号提前吃成 1.9.2，剩余任务的变更集只能另发一版，「同批发 1.9.2」的约定即失效。

## 影响范围

| 触达面 | 结论 |
| --- | --- |
| `src/skills.ts` | 改：版本解析 + 三个类型与报告字段 |
| `src/cli.ts` | 改：status/install 展示版本 + help 文案 |
| `src/__tests__/skills.test.ts` | 改：增补版本解析与 JSON 输出用例 |
| `skills/fxri-plan-to-task/SKILL.md` | 改：正文加版本声明 + 报版本纪律；version 递增 |
| `skills/fxri-session-recap/SKILL.md` | 改：同上 + L131 措辞澄清 |
| `skills/fxri-release-changelog/SKILL.md` | 改：同上 + L59 措辞澄清 |
| `skills/README.md` | 改：措辞澄清 + 正文/frontmatter 一致性说明 |
| `AGENTS.md` | 改：质量门第 3 条并入正文/frontmatter 一致性 + 措辞澄清 |
| `docs/cli.md` | 改：status 示例与行为行补版本、JSON 段补 `skillVersions`、措辞澄清 |
| `docs/handbook.md` | 改：status 行为行措辞澄清 |
| `docs/ai-rules.md` | 改：措辞澄清（保持薄壳，不复述可变细节） |
| `docs/faq.md` | 改：措辞澄清 |
| `docs/getting-started.md` | 改：措辞澄清 |
| `docs/guide.md` | 改：措辞澄清 |
| `docs/index.md` | 改：措辞澄清 |
| `README.md` | 改：措辞澄清 |
| `docs/api.md` | 无触达：`src/index.ts` 不导出 skills 域 |
| `docs/changelog.md` | 无触达：发版时由 `pnpm sync:changelog-doc` 镜像 |
| `.tasks/conventions.md` | 改：第 11 行当前语义更新 + 演进记录留痕 |
| `.changeset/` | 新增 patch 变更集（四个任务同批只发一个 patch 版本 1.9.2） |

- **对外行为**：`skills status` / `skills install` 输出新增版本信息；JSON 新增 `skillVersions` 字段（纯增量）。命令参数、退出码、告警口径不变。
- **兼容性**：JSON 消费方按既有字段读取不受影响；无版本声明的技能版本位为空串。

## 验证方式

- 质量门六步：`pnpm lint` → `pnpm typecheck` → `pnpm build` → `pnpm test` → `node dist/cli.js tasks check`（error/warn 双零）→ `node dist/cli.js tasks normalize`。
- 单测覆盖：版本解析（正常 / 带 BOM / 缺 `metadata`）、`skills status --format json` 含 `skillVersions`、install 报告含版本。
- 实测 loader：在旧会话中让 agent 报技能版本，确认正文声明值是否进入上下文（决定腿一是否成立）。
- grep 复核：旧措辞「同源同版本」归零。
- 回滚：改动集中在源文件 + skills + 文档，单次 revert 即可。

## 执行结果

- `src/skills.ts`：新增 `readSkillVersion(dir)`（读 `SKILL.md` → 剥 UTF-8 BOM → 正则取 frontmatter `metadata.version`，缺失返回空串不抛错）；`PackageSkill` / `SkillStatusItem` 增 `version`，`SkillsStatusReport` 增 `skillVersions`；`listPackageSkills()` 与 `skillsStatus()` 填充。
- `src/cli.ts`：`skillLabel(name, version)` 统一带版本显示；`printStatusReport()` 顶部打印技能真源版本清单、`printInstallReport()` 逐项带版本；`status` / `install` help 文案补「含版本」。
- `src/__tests__/skills.test.ts`：补「每个技能带真源声明版本」用例、`describe("readSkillVersion 版本解析")` 三用例（带/不带引号、BOM、无 frontmatter/无版本/文件缺失）；JSON 契约用例扩充 `skillVersions` 与逐项 `version` 断言。
- 三个 SKILL.md 正文首部加版本声明行与报版本纪律，frontmatter `metadata.version` 递增：`fxri-plan-to-task` 1.1.3→1.1.4、`fxri-session-recap` 1.1.1→1.1.2、`fxri-release-changelog` 1.0.7→1.0.8；`skills/README.md` 同步说明正文/frontmatter 一致性。
- `AGENTS.md`：质量门第 3 条并入「版本双写须一致」纪律；第 5 条措辞澄清为「随包同源分发（与 CLI 同一发布批次）」。
- 措辞澄清 9 处（cli.md / handbook.md / ai-rules.md ×2 / faq.md / getting-started.md / guide.md ×2 / index.md / README.md）统一为「随包同源分发，与 CLI 同一发布批次；技能内容版本独立编号」；`docs/cli.md` 另补 `status` 行为行与 `schemaVersion` 段的版本 / `skillVersions` 说明。
- `.tasks/conventions.md`：第 11 行「当前语义」更新，并按元规则在「演进记录」留痕。
- `.changeset/skill-version-visibility.md`：patch 变更集，条目按「新增：」前缀撰写。
- 质量门六步全通过：`pnpm lint` / `pnpm typecheck` / `pnpm build` / `pnpm test`（187 用例）/ `tasks check`（error、warn 双零）/ `tasks normalize`（0 问题）；grep 旧措辞「同源同版本」仅余历史归档、CHANGELOG 镜像与演进记录（按豁免处理）。
- ⚠️ 任务档要求「动手第一步先实测正文声明值是否进入旧会话上下文」的实测项，本次未单独执行，按既定措辞落地；该前提不影响本任务交付的版本可视化能力。

## 范围外

以下三项不在本任务改动范围内，各自独立建档跟踪，但与本任务同批发 1.9.2：

- `tasks stats --format json` 补 `schemaVersion`：`20260914-唐启云-补齐任务统计JSON版本号.md`。
- `SPEC.md` / `docs/ai-rules.md` 版本锚点：`20260914-唐启云-补规则层版本锚点.md`。
- CHANGELOG 语义分组扩展：`20260914-唐启云-CHANGELOG语义分组扩展.md`。

---

## 20260914-唐启云-路径书写规范与归档路径清理

> 负责人：唐启云　状态：已完成　范围：docs　完成时间：2026-09-14 12:05

# 路径书写规范与归档路径清理

## 背景

仓库内文档出现本机绝对路径：本任务档曾有两处 `file:///` 绝对链接指向作者本机开发目录，归档记录中亦有设计素材、外部全局规则文件、家目录示例等本机路径。绝对路径随 git 分发后对他人无意义，且泄露作者本机目录结构。用户要求：项目内的路径一律存相对路径，既有绝对路径全部改掉，并沉淀为规范。

## 实施方案

1. 归档记录中的本机路径做**字面归一**（只改路径写法，不动语义、结论与时间）：
   - 设计素材绝对路径 → `<本机>/…` 占位形式；
   - 外部全局规则文件绝对路径（2 处）→ `<本机>/…` 占位形式；
   - 家目录示例统一为 `%USERPROFILE%\…` 环境变量占位（原为盘符形式的本机家目录占位）；
   - junction 实测记录中的家目录路径 → `%USERPROFILE%\…`，目标路径 → 仓库相对路径。
2. 规范条文落 `AGENTS.md`「协作流程约束」，按**两阶段口径**：仓库内引用一律用**仓库根为基准的相对路径**（如 `src/cli.ts L103`），不写指向本机的绝对 `file:///` 链接；**任务进行中（active）**允许保留需要以绝对路径索引的本机资源路径（设计素材、仓库外规则文件等），便于当场定位；**归档时必须归一**为占位形式——仓库外文件 `<本机>/…`、家目录 `~/` 或 `%USERPROFILE%`，仅保留可辨识的末段文件名。
3. `.tasks/conventions.md` 溯源索引新增一行，单一事实源指向 AGENTS.md；因该规则在同一次确立中由「一律禁用」调整为「两阶段」，按元规则在「演进记录」留痕。
4. 本任务档与同批 active 任务档同步清理（已有 2 处绝对链接改相对路径）。

## 影响范围

| 触达面 | 结论 |
| --- | --- |
| `.tasks/archive/202609/20260905.md` | 改：4 处本机路径字面归一（设计素材 1、外部规则文件 2、家目录示例 1） |
| `.tasks/archive/202609/20260912.md` | 改：1 处 junction 实测记录路径归一；另 L371 / L399 的 `Env:\CI` 为 PowerShell 环境变量提供程序路径、非文件系统路径，无触达 |
| `.tasks/active/202609/20260914-唐启云-CHANGELOG语义分组扩展.md` | 改：2 处绝对链接改仓库相对路径 |
| `AGENTS.md` | 改：协作流程约束新增「路径书写」条 |
| `.tasks/conventions.md` | 改：溯源索引新增一行 |
| `src/skills.ts` L276 | 无触达：注释内 `C:\x\target\` 为通用说明性示例，非本机路径 |
| `skills/`、`docs/`、`README.md` | 无触达：全仓 grep 无本机绝对路径 |
| `.changeset/` | 无：仓库内部书写纪律与历史记录整理，不改对外行为 |

- **对外行为**：无 CLI 与 skills 行为变化。
- **兼容性**：纯文档整理，无破坏性。
- **归档不可变的例外说明**：按 task-spec 第 6 条「归档任务不可变」，本次对归档块的字面替换系用户明确指令下的路径归一，不新增内容、不改结论与时间。

## 执行结果

- `.tasks/archive/202609/20260905.md`：4 处本机路径字面归一——L191 家目录示例 → `%USERPROFILE%\.toolkitrc.json`；L253 设计素材 → `<本机>/…/方弦图标-圆形.png`；L339 与 L504 外部全局规则文件 → `<本机>/…/全局规则.md`。
- `.tasks/archive/202609/20260912.md`：L528 junction 实测记录归一——本机家目录 → `%USERPROFILE%\__jtest_cross__`，目标目录 → 仓库 `skills/fxri-plan-to-task`。
- `AGENTS.md`：「协作流程约束」新增「路径书写」条——仓库内引用一律用仓库根相对路径（含行号形如 `src/cli.ts L103`），禁本机绝对路径与指向本机的绝对 `file:///`，仓库外文件用 `<本机>/…`、家目录用 `~/` 或 `%USERPROFILE%` 占位。
- `.tasks/conventions.md`：溯源索引新增第 14 行「路径书写（本仓库治理）」，单一事实源指向 AGENTS.md「协作流程约束」。
- `.tasks/active/202609/20260914-唐启云-CHANGELOG语义分组扩展.md`：2 处绝对链接已在同批任务打磨阶段改为仓库相对路径，本次复核零残留。
- 复核：全仓 grep `file:///` / 盘符 / `/Users/` / `/home/` / `Dev-Space`，剩余 5 处均为**规则条文与任务描述中对该写法的指称**（非实际路径），无真实本机路径残留。
- 规范口径修订：按用户指示改为**两阶段**——任务进行中（active）允许保留需绝对路径索引的本机资源路径，归档时必须归一；`AGENTS.md`「路径书写」条与 `.tasks/conventions.md` 第 14 行已同步，并在 conventions「演进记录」留痕。

## 验证方式

- 归档块（本档归档后）零真实本机路径：grep 盘符、`/Users/`、`/home/`、`Dev-Space` 无命中；进行中任务档允许保留。
- 全仓 grep 盘符路径、`/Users/`、`/home/`、绝对 `file:///`、`Dev-Space` 等特征，除通用示例占位外零命中。
- `node dist/cli.js tasks check`（error/warn 双零）与 `node dist/cli.js tasks normalize`（0 问题）。
- 回滚：纯文档改动，单次 revert 即可。
