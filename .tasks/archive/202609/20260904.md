# 20260904 归档

> 本文件由 `toolkit tasks archive` 自动生成。

## 20260904-唐启云-新增游离任务文件软告警

> 负责人：唐启云　状态：已完成　范围：toolkit　完成时间：2026-09-04 15:40

# 新增游离任务文件软告警并发版 1.6.3

## 背景

任务文件若误放 `.tasks/` 根目录或漏建 `active/` 层，`tasks`/`check`/`archive` 全部读取不到，且无任何提示。本次补上该盲区。

## 方案

- `tasks check` 扫描 `.tasks/` 下游离任务文件：active/archive 之外、带 `{YYYYMMDD}-` 日期前缀的 `.md`（根目录与 `{YYYYMM}/` 一层子目录均覆盖）
- 命中输出软告警，提示移入 `active/{YYYYMM}/`
- 同步 README 校验说明；补单测覆盖根目录游离、漏层、README 不误报场景

## 影响范围

- 仅 `tasks check` 输出增加一种 warn，不改 API 与既有行为
- 版本 1.6.2 → 1.6.3（patch）

## 完成标准

- 单测通过，构建通过
- npm 发布 1.6.3，三远端推送完成

## 完成说明

- `tasks check` 新增游离任务文件软告警（validate.ts `strayTaskFiles`），根目录与漏建 active 层的 {YYYYMM}/ 子目录均覆盖
- 新增 validate-stray.test.ts 两用例，README 校验章节同步说明
- 经 changeset 升版 1.6.3，构建通过

---

## 20260904-唐启云-补充隐私脱敏禁用行为文档

> 负责人：唐启云　状态：已完成　范围：toolkit　完成时间：2026-09-04 15:05

# 补充隐私脱敏禁用行为文档并发版 1.6.2

## 背景

README 隐私脱敏章节的效果示例中，手机号因演示 `disable` 功能而未脱敏，读者易误解为手机号默认不脱敏。

## 方案

- README 自定义规则说明补充：被禁用规则不再参与匹配，敏感信息原样保留
- 效果示例表格拆为两行对照：手机号默认脱敏 `138****5678` / 禁用后原样保留
- 表格下方补充说明：禁用仅影响该类匹配，移除 `disable` 项后恢复默认脱敏

## 影响范围

- 仅文档变更（README.md），无代码改动
- 版本 1.6.1 → 1.6.2（patch）

## 完成标准

- README 描述与 `src/privacy/redact.ts` 实际行为一致
- 构建通过，npm 发布 1.6.2，三远端推送完成

## 完成说明

- README 效果示例拆为「默认未禁用 → `138****5678`」与「已禁用 → 原样保留」两行对照，并补充禁用行为说明
- 经 changeset 升版 1.6.2，构建通过

---

## 20260904-唐启云-skills与文档一致性修订

> 负责人：唐启云　状态：已完成　范围：文档　完成时间：2026-09-04 11:35

# skills 与文档一致性修订

## 方案概述

对照 toolkit 真实能力全面审查 skills 与文档后，确认结构一致，补齐 3 处缺失，不改任何代码。

## 实施内容

- 根 README.md 特性列表补第 4 条：零依赖 AI 技能包（含 `npx skills add fxri-net/toolkit` 安装命令）
- skills/README.md 方式二补 npm 包自带路径：已安装 @fxri/toolkit 的项目可从 `node_modules/@fxri/toolkit/skills/` 复制或链接
- skills/fxri-plan-to-task/SKILL.md 可选加速节补 `toolkit tasks normalize`（手工归档后核验归档块）

## 收尾项

- 任务校验通过后标记已完成并归档，与文档变更同一提交
- 建 patch 变更集随发版带出文档修订

---

## 20260904-唐启云-新增skills安装方案接入

> 负责人：唐启云　状态：已完成　范围：toolkit　完成时间：2026-09-04 10:50

# 新增 skills 安装方案接入与随包分发

## 方案概述

接入 vercel-labs/skills 安装器（`npx skills add fxri-net/toolkit`，兼容 75+ agent）作为推荐安装方式；同时把 `skills/` 加入 npm 包 `files` 实现随包分发；完成后发版、发布 npm、推送三仓库。

## 实施步骤

1. skills/README.md 安装章节重构：方式一 `npx skills`（含 skills-lock.json 提交约定与 `.claude/` 静默跳过的上游坑），方式二保留手工复制/软链
2. 根 README.md 方案落盘章节同步安装指引
3. 本方案落盘建档 → 完结 → 归档
4. package.json `files` 加入 `skills`，changeset 补充随包分发说明
5. `toolkit changelog version` 发版（minor：1.5.6 → 1.6.0）+ 构建 + 提交
6. `npm publish` 发布 @fxri/toolkit
7. 推送 origin / github / gitee 三远端（含版本 tag）

## 收尾项

- [x] 归档本任务后统一提交发版内容

---

## 20260904-唐启云-新增AI技能包skills

> 负责人：唐启云　状态：已完成　范围：toolkit　完成时间：2026-09-04 10:05

# 新增零依赖 AI 技能包 skills

## 方案概述

按 Agent Skills 开放标准（agentskills.io）将 toolkit 现有工作流提炼为通用技能包：纯 Markdown 规范，零工具依赖、零编程语言依赖，AI 仅凭文件读写即可完整执行。

## 变更清单

- `skills/fxri-plan-to-task/`：方案落盘工作流（先查后写 → 模板建档 → 状态机更新 → 自查校验 → 手工归档 → 归档提交同批），含 references/task-spec.md 通用规范与 assets 建档模板
- `skills/fxri-release-changelog/`：changesets 发版与多语言 CHANGELOG 维护，含 references/changelog-format.md 格式规范
- `skills/README.md`：索引、安装方式、fxri- 前缀命名约定、共存说明、发布前核对清单
- 两个 SKILL.md 末尾含「可选加速」节：检测到 @fxri/toolkit 时可用命令替代手工步骤，缺失不影响主流程
- 根 README.md 与 SPEC.md 增加交叉引用；changeset 记录 minor 变更

## 设计约束

- name 加 fxri- 组织前缀（对应 npm scope @fxri/），与目录名一致；description 含正向触发词与反向排除
- 主干 SKILL.md 控制在 200 行内，细节下沉 references/，可复制资产放 assets/
- 校验逻辑收口为自查清单、归档步骤收口为有序清单，避免与正文双源漂移
- SPEC.md 仍是任务文件格式的单一事实源，task-spec.md 为面向跨项目复用的通用精简版
