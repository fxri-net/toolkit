# 20260902 归档

> 本文件由 `toolkit tasks archive` 自动生成。

## 20260902-唐启云-跨环境兼容加固

> 负责人：唐启云　状态：已完成　范围：toolkit　完成时间：2026-09-02 23:32

# 跨环境兼容加固

## 目标

发布到 npm 后，确保 @fxri/toolkit 在 Windows/macOS/Linux、不同 Node 环境、CRLF/LF、CJS/ESM 消费方下均可用。

## 发现与修复

| 级别 | 问题 | 修复 |
|---|---|---|
| P1 | format.ts 依赖精确 `\n`，CRLF 文件下去重失效且产生混合换行 | 读入按原文件 EOL 归一化 LF 处理，写回还原原 EOL |
| P1 | 完全重复条目去重正则无 `/m`，只处理文件尾部 | 改为 `/gm`，任意位置连续重复均合并 |
| P1 | cli.ts 用 execSync 拼接字符串 + PATH 中 node，跨平台/含空格参数不稳 | 改 spawnSync(process.execPath, [bin, ...args])，退出码透传 |
| P1 | 无 exports 条件导出，CJS 消费方 require 在 Node 20 早期版本报 ERR_REQUIRE_ESM | package.json 补 exports（import→index.js/.d.ts，require→index.cjs/.d.cts） |
| P2 | 日期行后缀固定中文「发布」，en 语言也被写入中文 | ChangelogLanguage 增 released 字段，zh=发布 / en=released |

## 验证

- pnpm build 通过
- 临时断言脚本 9 项全过：CRLF 换行一致、中段/依赖去重、双语日期后缀、二次格式化幂等、CJS require 接线
- CLI 冒烟（help）正常

## 影响范围与回滚

- 仅 src/changelog/format.ts、src/changelog/languages/index.ts、src/cli.ts、package.json；行为不变
- 回滚：git 还原上述文件后重新 build 即可；线上包保持 1.0.0，未受影响

## 后续

- 发 patch 版 1.0.1 需 `npm version patch` + `pnpm run release`（另需用户确认）
