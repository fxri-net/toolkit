# 20260903 归档

> 本文件由 `toolkit tasks archive` 自动生成。

## 20260903-唐启云-toolkit-v151修复

> 负责人：唐启云　状态：已完成　范围：toolkit　完成时间：2026-09-03 18:30

# toolkit v1.5.1 归档头部与锁修复批次

修复 R1（E1-E5）并发布 1.5.1：

- E1 archive 合并保留既有归档文件自定义 header
- E2 归档/归一化排他锁陈旧检测（>10 分钟残留自动接管）
- E3 --target 非法值校验报错
- E4 漂移迁移到已含同名块的目标文件时告警
- E5 created/completed 真实日期校验

验证：门禁全过（单测 24/24）；npm 发布并推送三 remote。

---

## 20260903-唐启云-toolkit-v150能力增强

> 负责人：唐启云　状态：已完成　范围：toolkit　完成时间：2026-09-03 18:00

# toolkit v1.5.0 任务能力增强批次

对 @fxri/toolkit 做 A/B/C/D 四批增强并发布 1.5.0：

- A：任务校验与归档归一化增强（依赖引用归一、漂移块自动迁移、软告警补齐、completed 定宽、孤儿块检出、复选框扫描、导入冲突与截断告警、分隔符清理）
- B：语义与文档（过滤与 --view 引导、空态区分、README/SPEC 同步）
- C：工程化（vitest 单测 20 例、eslint/typecheck/CI、脱敏配置即时生效、changesets 自举发版、别名表修复）
- D：lxgl-web 使用侧（tasks:check/tasks:normalize 快捷脚本与只读审计）

验证：typecheck/lint/test/build 全过；端到端回归 20/20；文档一致性抽查通过。

---

## 20260903-唐启云-补充任务导出文档

> 负责人：唐启云　状态：已完成　范围：toolkit　完成时间：2026-09-03 14:30

# 补充任务导出字段与列别名文档

## 目标

README 补齐任务能力文档，便于用户与 AI 查阅：导出结构（终端/CSV/XLSX/JSON 各列与字段全集、排序与日期口径）、导入列别名内置表、库 API 增补。

## 实施步骤

1. README 新增「任务视图与导出结构」小节：统一字段全集表 + 四种输出格式列定义 + 排序/日期口径
2. README 新增「任务导入列别名（内置表）」小节：标准字段 → 内置别名表 + .toolkitrc.json 自定义示例
3. README 库 API：导入 query/printTaskBoard/exportTasks/importTasks 及示例

## 影响范围

- README.md

## 验证与回滚

- 与实现逐字段核对一致（query.ts/export.ts/import.ts）
- 回滚：git 还原

---

## 20260903-唐启云-tasks查询导入导出

> 负责人：唐启云　状态：已完成　范围：toolkit　完成时间：2026-09-03 14:24

# tasks 多视图查询与导入导出

## 目标

为 `toolkit tasks` 增加已归档查看能力与任务导入导出：

1. 视图：待完成（默认，兼容旧行为）/ 已归档 / 合并 all，状态分组展示 + 汇总
2. 过滤：--owner/--scope/--status（多值）/--date/--since/--until（date 与范围互斥）
3. 导出：CSV（UTF-8 BOM 超集列）/ XLSX（exceljs，待完成/已归档/汇总三 sheet）/ JSON（{summary, items}），扩展名驱动，文本走脱敏
4. 导入：--import <file> 兼容三种导出与外部列名（内置别名表 + .toolkitrc tasks.importColumns 自定义），--target active/archive、--dry-run、冲突自动加序号
5. 库 API：listArchivedTasks / queryTasks / printTaskBoard / exportTasks / importTasks

## 实施步骤

1. types.ts：TaskView/TaskRow/TaskFilter/TaskSummary/ImportOptions/ImportResult
2. query.ts：统一行读取（active+archive 复用 parseArchiveBlocks）、过滤、排序、汇总
3. list.ts：printTaskBoard 分组终端渲染
4. export.ts：toCSV/toJSON/toXLSX（exceljs 延迟加载）/exportTasks
5. import.ts：三种格式解析、列别名映射、active/archive 落盘、冲突序号、dry-run
6. cli.ts：--view/过滤/--export/--format/--import/--target 选项，导入模式独立
7. 依赖：exceljs（dependencies，仅 xlsx 路径加载）
8. README：命令、导入列别名内置表、库 API 文档同步

## 影响范围

- 新增 src/tasks/query.ts、export.ts、import.ts；修改 types/list/cli/index、package.json、README.md
- `toolkit tasks` 默认行为不变（兼容）

## 验证与回滚

- build + DTS 通过
- lxgl-web 真实归档冒烟：--view all 列出 49 归档、过滤正常；csv/xlsx/json 三种导出可读；三种导出产物回导成功（同名自动 -2/-3）
- 回滚：git 还原

---

## 20260903-唐启云-统一归档块解析修复

> 负责人：唐启云　状态：已完成　范围：toolkit　完成时间：2026-09-03 12:41

# 统一归档块解析修复正文误判与截断

## 目标

修复 tasks 归档块解析的两处同类缺陷，统一 check / fix / archive 三方对同一归档文件的块集合：

1. `normalize` 将归档正文里的 `## ` 小节误判为任务块（缺元数据误报），`--fix` 会把这些小节当块重排打散正文
2. `archive` 合并旧归档时，正则解析在正文 `---` + `## ` 处截断块体，再次归档合并会丢正文

## 实施步骤

1. 新增 `src/tasks/archive-block.ts`：统一 `parseArchiveBlocks`（任务块判定 = `## 标题` 后首个非空行为含「完成时间」的元数据行，正文小节归属前块正文）+ `renderBlock` / `buildMetaLine` / `normalizeCompleted`
2. `normalize.ts` 删除本地 `parseArchiveFile` 等重复实现，改用共享解析；`--fix` 写回时保留原文件行尾（CRLF 不整文件 diff）
3. `archive.ts` 的 `parseArchiveTasks` 改用共享解析 + 渲染；`normalizeCompleted` 移至共享模块，`src/index.ts` 同步导出位置

## 影响范围

- 新增 src/tasks/archive-block.ts；修改 src/tasks/normalize.ts、src/tasks/archive.ts、src/index.ts

## 验证与回滚

- `pnpm build` 通过（含 DTS）
- 用 lxgl-web 真实文档式归档冒烟：`parseArchiveTasks` 块数与正文完整（不再截断）；CRLF 副本 `--fix` 后块降序、正文完整、无 CR 残留、行尾保留 CRLF
- 回滚：git 还原即可

---

## 20260903-唐启云-changelog去commit前缀

> 负责人：唐启云　状态：已完成　范围：toolkit　完成时间：2026-09-03 12:15

# changelog format 去掉 commit hash 前缀

## 目标

changesets 默认 changelog-git 在变更集先提交后会往 CHANGELOG 条目写入 commit hash 前缀（如 `800a1cf: `），与中文 CHANGELOG 风格不一致。让 `toolkit changelog format`（及 `version` 自带的格式化）自动去掉该前缀。

## 实施步骤

1. `src/changelog/format.ts` 在标题替换后增加 `content.replace(/^- [0-9a-f]{7}: /gm, "- ")` 去掉 7 位 hex 前缀
2. 冒烟测试：带前缀条目被去除、无前缀条目不误伤

## 影响范围

- src/changelog/format.ts

## 验证与回滚

- `pnpm build` 通过（含 DTS）
- 临时 CHANGELOG 冒烟：`- 800a1cf: 修复` → `- 修复`，无前缀条目不变
- 回滚：git 还原即可

---

## 20260903-唐启云-normalize补锁并收敛文案

> 负责人：唐启云　状态：已完成　范围：toolkit　完成时间：2026-09-03 12:01

# normalize 归一化修复补排他锁并收敛定位文案

## 目标

1. 修复 `tasks normalize --fix` 缺少排他锁，与归档并发时可能互相覆盖归档文件
2. 收敛 README / SPEC / package.json 定位文案：融入「多人 + AI + 跨项目」，并将「多写者并发读写」修正为「多写者共享、工具内部写操作以排他锁防覆盖」

## 实施步骤

1. `src/tasks/normalize.ts` 的 `fixArchive` 增加与 `archive.ts` 相同的 `.archive.lock` 排他锁（`openSync("wx")` 抢占，`finally` 释放）
2. README.md / SPEC.md / package.json 定位文案收敛为「专为多人 + AI 跨项目协作打造」

## 影响范围

- src/tasks/normalize.ts、README.md、SPEC.md、package.json

## 验证与回滚

- `pnpm build` 通过（含 DTS 类型检查）
- 冒烟测试：临时目录 `normalize --fix` 补齐元数据成功，锁无残留
- 回滚：git 还原即可

---

## 20260903-唐启云-changelog全语言化与依赖升级

> 负责人：唐启云　状态：已完成　范围：toolkit　完成时间：2026-09-03 11:24

# changelog 全语言化与依赖升级

## 目标

1. 依赖升级：commander 14 → 15
2. changelog 全语言化：支持 `.toolkitrc.json` 配置自定义语言，且可覆盖内置 zh/en
3. 文档（SPEC/README）语言描述改为「全语言支持」

## 实施步骤

1. `pnpm up commander@15` 升级，适配 commander 15 的 `passThroughOptions` 变化（自有选项前置）
2. `src/cli.ts` 新增 `resolveLanguages()`，合并配置语言到内置（同名覆盖、新 key 追加）
3. SPEC.md / README.md 去掉 Node/PHP/Java/Go 语言列举，改为全语言支持；方案落盘规则完善

## 影响范围

- src/cli.ts、README.md、SPEC.md、package.json、pnpm-lock.yaml、CHANGELOG.md

## 验证与回滚

- 两项目 typecheck + build 通过；CLI 冒烟通过
- 全语言验证：追加 ja、覆盖内置 zh 均生效
- 回滚：git 还原 + `pnpm install` 即可

---

## 20260903-唐启云-发布v1.1.3

> 负责人：唐启云　状态：已完成　范围：toolkit　完成时间：2026-09-03 02:40

# 发布 v1.1.3

## 目标

将三处文档的「归档与提交约束」说明作为 1.1.3 patch 发布，验证单提交流程。

## 执行记录

- 写 patch 变更集并经 changelog version 生成 1.1.3
- README/.tasks/SPEC 文档补充「归档与提交约束」
- 一次性 commit + tag v1.1.3（先归档再提交）
- pnpm build + npm publish --dry-run 预检后正式 publish

---

## 20260903-唐启云-发布v1.1.2

> 负责人：唐启云　状态：已完成　范围：toolkit　完成时间：2026-09-03 02:22

# 发布 v1.1.2

## 目标

将 package.json repository 字段（GitHub 地址）作为 1.1.2 patch 发布。

## 执行记录

- package.json 新增 repository 指向 github.com/fxri-net/toolkit
- 写 patch 变更集并经 changelog version 生成 1.1.2
- 提交 ce2f94a 并打 tag v1.1.2
- npm publish --dry-run 预检通过（repository 已收录为 git+https 形式）
- 先归档本任务，再正式 publish

---

## 20260903-唐启云-发布v1.1.1

> 负责人：唐启云　状态：已完成　范围：toolkit　完成时间：2026-09-03 02:08

# 发布 v1.1.1

## 目标

将 CLI 引入 Commander 完善帮助功能（--help/--version/子命令帮助）作为 1.1.1 patch 发布。

## 执行记录

- 写 patch 变更集并经 changelog version 生成 1.1.1（CHANGELOG 顶部插入 1.1.1 段）
- 清理 changesets 重写导致的旧版本标题后空行，统一格式
- 提交 4ddba92 并打 tag v1.1.1
- pnpm build + npm publish --dry-run 预检通过（15 文件 / 16.2 kB）
- 先归档本任务，再正式 publish

---

## 20260903-唐启云-发布v1.1.0

> 负责人：唐启云　状态：已完成　范围：toolkit　完成时间：2026-09-03 01:45

# 发布 v1.1.0

## 目标

将隐私脱敏功能、归档降序排序、日期补全修复作为 1.1.0 minor 发布，走 changesets 流程并 dogfood 验证。

## 执行记录

- 写 minor 变更集并经 changelog version 生成 1.1.0（CHANGELOG 顶部插入 1.1.0 段）
- 清理 changesets 重写导致的旧版本标题后空行，统一格式
- 清理 cli.ts 中过程性解释注释
- README 补充 node18 兼容性实测表
- 提交 7dc4587 并打 tag v1.1.0
- pnpm build + npm publish --dry-run 预检通过（15 文件 / 16.3 kB）
- 先归档本任务，再正式 publish

---

## 20260903-唐启云-调整归档排序

> 负责人：唐启云　状态：已完成　范围：toolkit　完成时间：2026-09-03 01:01

# 调整归档排序

## 目标

统一 completed 定宽规范化后再排序；归档文件内改为降序（最新在前）；历史归档同步重排。

## 执行记录

- archive.ts 新增 normalizeCompleted：非定宽 completed（如 2026-9-2 9:00）补零为 YYYY-MM-DD HH:mm，用于归组/排序/归档显示
- 归档文件内排序改为 completed 降序（最新在前）
- 同步 SPEC.md 与 .tasks/README.md 的排序描述（升序 → 降序）
- 历史归档 20260902.md / 20260903.md 重排为降序
- fixture 断言 6 项通过（降序、非定宽规范化、合并回归、幂等）

---

## 20260903-唐启云-发布v1.0.2

> 负责人：唐启云　状态：已完成　范围：toolkit　完成时间：2026-09-03 00:47

# 发布 v1.0.2

## 目标

将 README「方案落盘（任务区）」章节作为 1.0.2 patch 发布，走 changesets 流程并 dogfood 验证。

## 执行记录

- 写 patch 变更集并经 changelog version 生成 1.0.2（CHANGELOG 顶部插入 1.0.2 段）
- 手工清理 changesets 重写导致的 1.0.1/1.0.0 段重复日期行
- 提交 2a1336c 并打 tag v1.0.2
- pnpm build + npm publish --dry-run 预检通过（15 文件 / 14.1 kB，README 含新章节）
- 先归档本任务，再正式 publish

---

## 20260903-唐启云-验证node18兼容性

> 负责人：唐启云　状态：已完成　范围：toolkit　完成时间：2026-09-03 00:34

# 验证 node18 兼容性

## 目标

确认 @fxri/toolkit 在只装 node18 的机器上能否运行，决定是否将最低支持版本降至 18。

## 执行与结果

- 将 engines / tsup target / README 临时降为 node18 并重建产物
- 全量测试（help、tasks list/archive、changelog format CRLF+双语+幂等、ESM/CJS 库）：node20.20.2 13/13 通过
- node18.20.8：tasks/format/库 API 域 11/13 通过
- changelog 域在 node18 失败：@changesets/write@0.4.0（CJS）require 纯 ESM 的 human-id@4 → ERR_REQUIRE_ESM；Node 18 不支持 require(esm)，该能力需 Node ≥20.19 / ≥22.12。属上游 changesets 生态限制，非本项目代码

## 决策

保持最低支持版本 Node ≥20（C 方案），回退全部改动，工作区与 HEAD 一致
